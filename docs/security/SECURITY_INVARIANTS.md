# Frontera Security Invariants

- Status: canonical. This is the single authoritative statement of what Frontera guarantees, where each guarantee stops, and what is not yet implemented.
- Established by: Security & Containment Architecture track, Prompt 1.
- Baseline evidence: `SECURITY_CONTAINMENT_BASELINE_AUDIT.md` (Prompt 0).
- Companion documents: `docs/security/NO_BYPASS_AUTHORITY_CONTROLLED_EXECUTION.md` (canonical: the complete effect-path inventory, what authorizes each effect, and the resource-centric no-bypass analysis), `docs/security/TRUST_BOUNDARIES_AND_PRIVILEGED_ASSETS.md` (canonical: trust domains, TCB, privileged assets, authority-write and effect maps, chokepoints, bypass primitives), `docs/security/AUTHORITATIVE_GRANT_STORE.md` (canonical: how bounded grants and revocations are persisted, what that guarantees, and where it stops), `docs/security/THREAT_MODEL_V1.md`, `docs/security/AGENT_PASSPORT_WEB_THREAT_MODEL.md` (the SaaS surface), `docs/security/SECURITY_HARDENING_V1.md`, `docs/kernel/AOC_KERNEL_INVARIANTS_V1.md` (Kernel-scoped), `docs/architecture/ADR-AUTHORITY-CONTROL-LAYERING.md`.

---

## 1. Purpose

This document defines, for every security property Frontera claims:

1. **the guarantee** — what is actually true;
2. **the scope** — the exact path, layer, component or boundary it holds within;
3. **the enforcement mechanism** — how it is prevented from regressing;
4. **the evidence** — the file, line, or test that proves it;
5. **the boundary** — where the guarantee stops being true;
6. **the implementation status** — enforced, partially enforced, or not implemented.

It exists because the repository's guarantees are unusually strong **within specific paths** and absent outside them. Stating a path-local guarantee without its scope is the most likely way this project would accidentally overclaim. Every entry below therefore carries a scope, and the scope is part of the invariant — not a footnote to it.

`docs/kernel/AOC_KERNEL_INVARIANTS_V1.md` remains authoritative for the Kernel's own ten decision-engine invariants and is not superseded by this document; it is Kernel-scoped, and this document is the cross-cutting artifact that places it alongside every other guarantee.

---

## 2. Security Model

Three distinctions govern how this repository reasons about authority.

```
Intelligence  !=  Authority     an actor that can determine what to do has not thereby been permitted to do it
Capability    !=  Permission    an actor that can technically reach an effect has not thereby been authorized to produce it
Intent        !=  Mandate       an actor's declared purpose is not a grant; a mandate is held by a trusted store, not asserted by the actor
```

The security objective is **not** "prevent an agent from ever becoming malicious." It is:

> prevent malicious intent from automatically obtaining legitimate authority or technical capability to produce privileged effects.

That objective has two halves, and this repository is strong in one and absent in the other. They must never be collapsed:

| | Question it answers | Frontera today |
|---|---|---|
| **Authorization security** | Is this actor legitimately allowed to perform this *declared* action? | Mature. Single decision producer, narrowing-only composition, fail-closed bounded-grant exercise, structural boundary tests that fail CI. |
| **Containment security** | What can this actor technically reach, execute, mutate, read, exfiltrate or destroy if it behaves maliciously? | Essentially absent. No process isolation, no egress control, no capability dropping, no aggregate bounding, no durable kill switch. |

Every invariant in §4 is an authorization-security or structural-hygiene invariant. Every entry in §7 is a containment-security invariant, and every one of them is unimplemented or partial.

---

## 3. Scope Vocabulary

Every invariant carries exactly one scope.

| Scope | Meaning |
|---|---|
| **SYSTEM-WIDE** | Proven to hold across every relevant production path in this repository. Where a named exception exists, the invariant is not system-wide and must be scoped down instead. |
| **LAYER-LOCAL** | Holds only within named architectural layers (`ADR-AUTHORITY-CONTROL-LAYERING.md` A–G). Says nothing about other layers. |
| **PATH-LOCAL** | Holds only for one end-to-end execution/effect path. Says nothing about the other effect paths in §5. |
| **COMPONENT-LOCAL** | Holds only for a specific named service, store, module or application. |
| **DEPLOYMENT-CONTRACT** | Depends on infrastructure or operator configuration outside this repository's control. The repository can state the requirement; it cannot enforce it. |
| **ASPIRATIONAL-UNIMPLEMENTED** | A desired property that is not currently enforced by anything. Recorded so it cannot be mistaken for a guarantee. |

Enforcement strength is stated separately, and more than one value may apply:

| Enforcement | Meaning |
|---|---|
| **STRUCTURAL** | A build- or test-time check fails if the property is violated, independent of runtime input. |
| **RUNTIME** | Production code checks the condition at runtime and fails closed. |
| **TYPE-LEVEL** | The TypeScript type system makes the violation unexpressible. |
| **TEST-ENFORCED** | A test asserts the behaviour; `npm test` runs on every PR and push to `main` (`.github/workflows/ci.yml`). |
| **DOCUMENTED** | Stated in an ADR, doc comment or design document, with no structural or runtime mechanism proving it. |
| **DEPLOYMENT-DEPENDENT** | Holds only if the operator configures the deployment as documented. |
| **NOT IMPLEMENTED** | No mechanism exists. |

`DOCUMENTED` alone is never a guarantee. It records intent.

---

## 4. Enforced Invariants

### 4.1 Decision production and authority composition

| ID | Invariant | Scope | Enforcement | Evidence | Boundary |
|---|---|---|---|---|---|
| SEC-INV-001 | The Kernel is the only producer of a Frontera **authorization decision**: contributing layers annotate or narrow, and no other component forms an allow. | LAYER-LOCAL to the `src/kernel` + `src/features` + `src/enterprise` authorization pipeline | STRUCTURAL + TEST-ENFORCED + DOCUMENTED | `ADR-AUTHORITY-CONTROL-LAYERING.md` §4; `src/enterprise/__tests__/structural-boundaries.test.ts:300-505`; `src/features/execution-runtime/tests/execution-layer-boundaries.test.ts:160-223` | **Not repository-wide.** `packages/agent-governance`'s `evaluateAgentRuntimeGuard` produces its own independent `allow`/`deny`/`require_human_approval` outcome and is never consulted by the Kernel. The Sovereign Access path makes its own lifecycle determination (`assertActive`, `src/enterprise/access-governance/lifecycle.ts:20`). Both are separate decision producers outside this invariant. |
| SEC-INV-002 | Narrowing-only composition: the governed-authority, context, obligation and grant steps may deny, annotate or withhold, but may not widen an outcome or create an allow. | LAYER-LOCAL to the Kernel pipeline | STRUCTURAL + TEST-ENFORCED | `AocKernel.ts:357,366,376,386`; `structural-boundaries.test.ts:428,499-505` asserts `applyGrantStep` reads none of `result.status`/`reasonCodes`/`summary`/`policies` | Says nothing about what an already-allowed decision's executor then does — see SEC-INV-009. |
| SEC-INV-003 | No action is allowed for an unrecognized actor. | PATH-LOCAL to `AocKernel.evaluate()` / `enforce()` | RUNTIME + TEST-ENFORCED | `assertRecognitionPrecedesAllow`, `src/kernel/orchestration/kernel-invariants.ts:14-20`; asserted on every call; `AOC_KERNEL_INVARIANTS_V1.md` §1 | Recognition state is held in process memory by default (see SEC-TRUST-003). |
| SEC-INV-004 | Every terminal Kernel result carries machine-readable reason codes. | PATH-LOCAL to `AocKernel.evaluate()` / `enforce()` | RUNTIME + TEST-ENFORCED | `assertReasonCodesPresent`, `kernel-invariants.ts:23-27` | — |
| SEC-INV-005 | Evaluation never mutates the caller's request. | PATH-LOCAL to `AocKernel.evaluate()` / `enforce()` | RUNTIME + TEST-ENFORCED | `cloneKernelEvaluationRequest` + `deepEqual` (`Object.is`, Date-by-epoch), `kernel-invariants.ts:29-59` | — |
| SEC-INV-006 | An optional capability port that is not configured leaves behaviour byte-identical to that layer not existing. | LAYER-LOCAL to Kernel optional ports | TEST-ENFORCED + DOCUMENTED | `ADR-AUTHORITY-CONTROL-LAYERING.md` §6; `src/kernel/__tests__/characterization/*-capability-absent.test.ts` | — |
| SEC-INV-007 | Grant eligibility reported by `evaluate()` is a projection only: no grant is issued, no store is written, and the Governance Record is unchanged. | PATH-LOCAL to `AocKernel.evaluate()` | STRUCTURAL + TEST-ENFORCED | `structural-boundaries.test.ts:486-497` — no production Kernel source may import the issuance path or the grant store, and `KernelEvaluationResult` may not carry a `BoundedGrant` | — |

### 4.2 Effect-path invariants

These are the highest overclaim risk in the repository. Each is scoped to exactly one path in §5.

| ID | Invariant | Scope | Enforcement | Evidence | Boundary |
|---|---|---|---|---|---|
| SEC-INV-008 | The evaluation path produces no external effect: it invokes no executor, no adapter, and performs no outbound I/O. | PATH-LOCAL to `AocKernel.evaluate()` and `POST /api/governance/evaluate` | STRUCTURAL + RUNTIME | `AocKernel.ts:311-390` contains no invocation site; `src/kernel` production sources import only `crypto`; `src/enterprise/__tests__/security-invariants.test.ts` | Persisting the Governance Record is a local durable write, not an external effect. |
| SEC-INV-009 | `AocKernel.enforce()` invokes the caller-supplied executor **only** when preflight resolves to `execute_allowed`; every other outcome returns without calling it. | PATH-LOCAL to `AocKernel.enforce()` | RUNTIME + TEST-ENFORCED | `GuardedExecutionService.run`, `src/features/action-enforcement/services/guarded-execution-service.ts:35-105` — the single `await execute()` at `:105` is preceded by the unconditional return at `:39-91` | This is **decision-time** authorization. See SEC-INV-010. |
| **SEC-INV-010** | **LIMIT.** `AocKernel.enforce()` does **not** bind the executor's actual external effect to the declared `ActionDescriptor`. The executor is an opaque closure; nothing structural, cryptographic or runtime-level verifies that what it does matches what was authorized. | PATH-LOCAL to `AocKernel.enforce()` | NOT IMPLEMENTED — stated as a first-class boundary, not a defect | `guarded-execution-service.ts:105` accepts `() => Promise<T> \| T` and inspects nothing; `src/enterprise/execution-governance/service.ts:56-64` records the accepted architectural reading | A Governance Record produced through `enforce()` attests the **declaration**, not the effect. Effect-time binding is what the bounded-grant path provides instead (SEC-INV-011). Closing SC-003 is a claiming decision, not a Kernel redesign: grants deliberately do not gate `enforce()`. |
| SEC-INV-011 | No `ExecutionAdapter` call occurs unless the authoritative bounded grant has been re-read from the store and a usable exercise assessment covers the exact attempted action at that instant. Since the execution adapter registry there are **two** enumerated invocation sites — the gate, and the composite it may route through — and the second is reachable only through the first. | **PATH-LOCAL** to bounded-grant exercise (`GrantExecutionService` / `AuthorityControlledExecutionService`) | STRUCTURAL + RUNTIME + TEST-ENFORCED | `grant-execution-service.ts:154-198`; `src/features/execution-runtime/tests/execution-exercise.test.ts` counts adapter invocations (`callCount === 0` on every refusal row, `=== 1` on the valid row); call-site enumeration in `security-invariants.test.ts` and `no-bypass-effect-paths.test.ts`, which also pin that the registry runs after routing and after the emergency-control gate | **This is not a system-wide guarantee.** It does not apply to `AocKernel.enforce()` (SEC-INV-010), to the Sovereign Access provider path (SEC-INV-019), to the Content Protection provider path (a **second** separate provider authority model, `NO_BYPASS_AUTHORITY_CONTROLLED_EXECUTION.md` NB-003), or to any application code that calls a provider directly (SEC-TRUST-004). Eight of the repository's fifty-five enumerated effect paths are bounded-grant controlled — EP-049 being the customer HTTP entry onto this same path (SEC-INV-059), EP-050 the Generic HTTP adapter's outbound request below it (SEC-INV-062 … SEC-INV-069), and EP-051 … EP-053 the P7 exercise-control reservation writes, which are local authority-state writes inside this same gate rather than provider effects (SEC-INV-070 … SEC-INV-079) — see that document's §5. |
| SEC-INV-012 | The grant is read, never received: a caller supplies a grant **id** and nothing else about the grant, and `subject` and `notAfter` crossing to the adapter are read from the trusted store, never from the request. | PATH-LOCAL to bounded-grant exercise | TYPE-LEVEL + RUNTIME + TEST-ENFORCED | `GrantExerciseRequest` shape; `grant-execution-service.ts:37-44, 184-194` | — |
| SEC-INV-013 | Revocation and expiry are visible to the very next exercise: the store is re-read on every attempt, with no cache, no fast path and no sweeper, and the instant is sampled after the awaited read. | PATH-LOCAL to bounded-grant exercise | RUNTIME + TEST-ENFORCED | `grant-execution-service.ts:154,158`; `grant-exercise-assessment.ts:123,128-130`; `execution-exercise.test.ts:185` | Revocation of the *underlying recognition capability token* is in-process memory and not durable (SEC-TRUST-003). |
| SEC-INV-014 | No free-form payload, blob or opaque reference crosses the execution adapter boundary: the validated action **is** the payload. | PATH-LOCAL to bounded-grant exercise | STRUCTURAL + TYPE-LEVEL + TEST-ENFORCED | `execution-adapter-port.ts:40-49`; `execution-layer-boundaries.test.ts:352-396` bans `payload\|blob\|rawBody\|opaqueRef\|commandRef` and whitelists exactly 17 field names | Constrains the **channel**. The adapter implementation itself is trusted code and is not verified. |
| SEC-INV-015 | Bound comparison is fail-closed: absence on either side is a refusal, `incomparable` is treated exactly as `broader`, a malformed request is refused before any comparison, and every failing axis is reported. | PATH-LOCAL to bounded-grant exercise | RUNTIME + TEST-ENFORCED | `grant-exercise-assessment.ts:84-87, 110-112, 142-179` | — |
| SEC-INV-016 | Grant issuance checks run inside the store's own commit boundary: the guard is **synchronous by type**, so no `await` can interleave between the read that decides and the write that records. | PATH-LOCAL to grant issuance | TYPE-LEVEL + TEST-ENFORCED | `grant-store-port.ts` `commitGuard: () => GrantCommitPrecondition`; `grant-runtime/tests/grant-transaction-boundary.test.ts` | — |
| SEC-INV-017 | Any change to the authority binding between measurement and commit refuses issuance — equality, not containment. | PATH-LOCAL to authority-controlled issuance | RUNTIME + TEST-ENFORCED | `src/enterprise/execution-governance/service.ts:197-234` | — |
| SEC-INV-018 | Attenuation only: an issued grant is equal to or narrower than the authority it derives from on every axis, proven against the artifact rather than the process that made it. | LAYER-LOCAL to layer E | RUNTIME + TEST-ENFORCED | `grantScopeIsWithin`; `grant-runtime/tests/grant-attenuation.test.ts`; `ADR-AUTHORITY-CONTROL-LAYERING.md` §5 | — |
| **SEC-INV-019** | **BOUNDARY.** Sovereign Access provider execution is a **separate authority-bearing path** with its own grant semantics (`EnterpriseAccessGrant`, authoritative store read plus `assertActive`). It does not pass through the Kernel and does not perform a bounded-grant exercise. | PATH-LOCAL to Sovereign Access | RUNTIME (its own model) + DOCUMENTED | `src/enterprise/access-governance/service.ts:252-296` (`requestProviderCredential`), `:281`; `pinata-revocation-enforcement.ts:110`; `lifecycle.ts:20` | SEC-INV-011 **does not apply to this path.** **Prompt 3 decided: EXCEPTED, not converged** (`NO_BYPASS_AUTHORITY_CONTROLLED_EXECUTION.md` §9.4, §20.2 — SC-002 REFINED, STILL OPEN). Not HTTP-reachable — in-process trusted host code only, and not reachable at all from a published-package consumer (NB-010). |

### 4.3 Capability and layer hygiene

| ID | Invariant | Scope | Enforcement | Evidence | Boundary |
|---|---|---|---|---|---|
| SEC-INV-020 | These layers perform no I/O of their own: no `node:fs`, `node:http`, `node:net`, `node:child_process`, `fetch(`, or database client. | LAYER-LOCAL to context-resolution runtime (C), obligation runtime (D), grant runtime (E), execution runtime | STRUCTURAL + TEST-ENFORCED | `context-layer-boundaries.test.ts:62`; `obligation-layer-boundaries.test.ts:80`; `grant-layer-boundaries.test.ts:133-149`; `execution-layer-boundaries.test.ts:149-157` | **Four modules only.** `src/features/action-enforcement`, the rest of `src/enterprise`, all of `packages/` and all of `apps/` have no such test. See SEC-INV-U08. |
| SEC-INV-021 | No dynamic code evaluation: no `eval(`, `new Function(`, dynamic `import(`, or `vm`. | LAYER-LOCAL to layers C, D, E, the execution runtime, **and the Kernel** | STRUCTURAL + TEST-ENFORCED | `context-layer-boundaries.test.ts:118`; `obligation-layer-boundaries.test.ts:160`; `grant-layer-boundaries.test.ts:228`; `execution-layer-boundaries.test.ts:270-279`; Kernel coverage added in `security-invariants.test.ts` | Not proven for `packages/` or `apps/`. `apps/agent-passport-web` contains one constant-specifier `require('stripe')` **and six constant-specifier `await import(...)` calls** (three of `'stripe'`, three of its own modules). None takes a caller-influenced specifier, so none is a dynamic-code-execution primitive — see `NO_BYPASS_AUTHORITY_CONTROLLED_EXECUTION.md` §4.2. |
| SEC-INV-022 | No ambient clock and no ambient randomness: every instant and identifier is injected. | LAYER-LOCAL to layers C, D, E and the execution runtime | STRUCTURAL + TEST-ENFORCED | `execution-layer-boundaries.test.ts:281-309`; sibling assertions in the grant/context/obligation suites | The Kernel deliberately defaults to a real clock and `crypto.randomUUID()` (`AocKernel.ts:181-187`), overridable by injection. |
| SEC-INV-023 | Correctness never depends on a background job having run: no timer, scheduler, cron, queue, worker or sweeper. | LAYER-LOCAL to the execution runtime (and, for expiry semantics, layer E) | STRUCTURAL + TEST-ENFORCED | `execution-layer-boundaries.test.ts:291-299` | — |
| SEC-INV-024 | No AI, model or inference dependency in the authorization path. | **DOCUMENTED SYSTEM-WIDE; TEST-ENFORCED only in layer E, the execution runtime, execution-governance, and the Kernel** | DOCUMENTED (system-wide) + STRUCTURAL + TEST-ENFORCED (four modules) | `ADR-AUTHORITY-CONTROL-LAYERING.md` "layer G may not appear anywhere in the authorization path, under any configuration"; `grant-layer-boundaries.test.ts:208`; `execution-layer-boundaries.test.ts:214-222`; `authority-controlled-execution-boundaries.test.ts`; Kernel coverage added in `security-invariants.test.ts` | **The ADR's claim is broader than the tests.** Layers B (policy / action-enforcement), C and D carry no AI-ban test. Stated as documented intent there, not as a structural guarantee. |
| SEC-INV-025 | No customer transaction signing key is held, requested, named or expressible: no wallet, mnemonic, `signTransaction`, nonce, sequence number, chain identifier or ledger client. | LAYER-LOCAL to layer E and the execution runtime, plus execution-governance — and an architectural non-goal repository-wide | STRUCTURAL + TEST-ENFORCED + DOCUMENTED | `execution-layer-boundaries.test.ts:226-248`; `structural-boundaries.test.ts:480-484`; `authority-controlled-execution-boundaries.test.ts`; `TARGET_AUTHORITY_CONTROL_ARCHITECTURE.md` §10 "no password or key custody for target systems"; `packages/tokenization-mandate/src/enterprise-tokenization-execution.ts:29` | The vocabulary ban is test-enforced in the grant/execution/execution-governance modules. Repository-wide absence of chain clients is a verified present-state fact and an architectural commitment, not a build-time check over every directory. |
| SEC-INV-026 | No external token format is adopted: a bounded grant is an internal typed record held by a trusted store; a caller never holds one and therefore never presents one. | LAYER-LOCAL to layer E and the execution runtime | STRUCTURAL + TEST-ENFORCED | `execution-layer-boundaries.test.ts:250-258` bans `jwt\|macaroon\|UCAN\|oauth\|bearer\|signed_url`; `bounded-grant.ts:34-40` | — |

### 4.4 Surface, secrets and authoritative state

| ID | Invariant | Scope | Enforcement | Evidence | Boundary |
|---|---|---|---|---|---|
| SEC-INV-027 | No caller-facing surface exists for grants: no route issues, extends, revokes or exercises a grant, and the execution runtime exports no handler, route, controller or endpoint. | PATH-LOCAL to bounded-grant execution; COMPONENT-LOCAL to the Enterprise HTTP adapter | STRUCTURAL + TEST-ENFORCED | `execution-layer-boundaries.test.ts:399-408`; `composition-root.ts:112-127`; the adapter's 8 route families, `node-http-adapter.ts:137,155,163,227,231,298,306,339` | Composing grant-aware execution is reachable from trusted in-process host code, by design. **Since P5** one customer route, `POST /api/governed-actions`, reaches the bounded-grant path — at the top, through customer admission and the Governed Action Orchestrator — and still issues, extends, revokes or exercises no grant *on the caller's behalf as an artifact*: grants stay server-side and no response carries one (SEC-INV-057, SEC-INV-059). |
| SEC-INV-028 | Provider credentials and API keys never appear on the public composition surface, and no hardcoded credential fallback exists. | COMPONENT-LOCAL to the Enterprise Host | STRUCTURAL + TYPE-LEVEL + TEST-ENFORCED | `structural-boundaries.test.ts:256-299` (5 assertions); `PublicEnterpriseConfiguration` typing | Does **not** cover `apps/agent-passport-web`, which reads `process.env` ad hoc across `lib/` and route handlers. |
| SEC-INV-029 | The `GovernanceStore` and `AgentPassportStore` interfaces expose no public update or delete method. | COMPONENT-LOCAL to those two interfaces | STRUCTURAL + TEST-ENFORCED | `structural-boundaries.test.ts:169-173, 238-242` | **Two interfaces only.** Other stores (authority, assurance, mandates, access grants) perform `UPDATE` on status/counters, re-sealing the row digest. Deletion is a filesystem/DBA capability outside the software boundary. |
| SEC-INV-030 | Sensitive-keyed values are redacted before persistence, before digesting and before logging, so the digest attests to the sanitized record actually stored. | COMPONENT-LOCAL to the Governance Store | RUNTIME + TEST-ENFORCED | `redaction.ts:1-5, 78-86`; applied at `projection.ts:76,91,200,300` and `store-common.ts:169,189,212` | **Key-name matching only.** A secret under a non-matching key is persisted verbatim and, being inside the digest, cannot be removed without breaking the chain. See SEC-INV-U07. |
| SEC-INV-031 | Stored authority artifacts are integrity-checked fail-closed at read time: a row whose fields no longer match its digest fails the read rather than being skipped or repaired. | COMPONENT-LOCAL to the governed-authority store and bounded-grant exercise | RUNTIME + TEST-ENFORCED | `sqlite-authority-store.ts:646,671` (`assertReservationIntegrity` / `assertEncumbranceIntegrity`); `grant-exercise-assessment.ts:119` | **Integrity, not authenticity.** Unkeyed SHA-256 — see SEC-TRUST-002. The Governance Store re-verifies only on explicit `verify`, not on `get` (`THREAT_MODEL_V1.md` §7.1). |
| SEC-INV-032 | Durable Kernel Authority writes require both a system context and an operator context. | COMPONENT-LOCAL to `KernelAuthorityProvisioningService` | RUNTIME + DOCUMENTED | `src/enterprise/kernel-authority/provisioning-service.ts:51-60`; event-sourced append-only rules in `append-rules.ts` | **Not the pattern everywhere.** `PolicyPackRegistry.savePack` / `saveVersion` / `activatePolicyPackVersion` (`policy-pack-registry.ts:81,121,129`) carry no caller identity at all and are protected only by not being exposed. |
| SEC-INV-033 | The reason-code vocabularies (authorization, obligation, grant issuance, grant exercise, provider failure, emergency control, exercise control) are pairwise disjoint; every exercise code carries the `GRANT_EXERCISE_` prefix, every interlock code the `EMERGENCY_CONTROL_` prefix and every aggregate-control code the `EXERCISE_CONTROL_` prefix. | SYSTEM-WIDE across those vocabularies | STRUCTURAL + TEST-ENFORCED | `execution-layer-boundaries.test.ts` (four vocabularies, plus exercise control); `src/features/emergency-control-runtime/tests/emergency-control-boundaries.test.ts` (the interlock against all five others); `src/features/exercise-control-runtime/tests/exercise-control-boundaries.test.ts` (exercise control against six); `structural-boundaries.test.ts` (exercise control against the governed-action and authority-binding vocabularies) | — |
| SEC-INV-034 | Assurance frameworks are registered at composition only, validated structurally, immutable per version, and the registry is frozen before traffic; no HTTP path writes a framework. | COMPONENT-LOCAL to the Assurance Runtime | RUNTIME + DOCUMENTED | `THREAT_MODEL_V1.md` §7.11 | — |
| SEC-INV-035 | The durable bounded-grant store persists grants and revocations to **one** database file, in **one** transaction, under `synchronous = FULL`, so an acknowledged revocation cannot be the half a crash or restart loses. Restart may remove authority; it can never add any. | COMPONENT-LOCAL to `createSqliteBoundedGrantStore` | RUNTIME + TEST-ENFORCED | `src/enterprise/bounded-grant-store/sqlite-bounded-grant-store.ts`; `bounded-grant-store-durability.test.ts` (`revoke-survives`, `restart-monotonic`, `revoke-commit`) | **Conditional on configuration.** The durable store is selected when `persistence.provider === 'sqlite'`; the default provider is `memory`, where grants and revocations are lost together on restart — which fails closed. `AUTHORITATIVE_GRANT_STORE.md` §15, GS-003, D-GS5 |
| SEC-INV-036 | Persisted grant **and** revocation records are integrity-verified on every authorization-sensitive read — record digest, artifact digest, canonical round-trip, identity and schema version — and state that fails validation yields no usable grant. It is never repaired, skipped or normalized. | COMPONENT-LOCAL to `createSqliteBoundedGrantStore` | RUNTIME + TEST-ENFORCED | `verifiedGrant`, `verifiedRevocation`, `currentRevocation`; nine fail-closed cases in `bounded-grant-store-durability.test.ts` | **Unkeyed digests.** This detects mutation; it does not stop a writer who can re-seal (SEC-TRUST-002, SEC-INV-U01, GS-001). Extends SEC-INV-031's posture to the bounded-grant store, and to revocations, which previously carried no integrity metadata at all |
| SEC-INV-037 | A grant and its revocation record vouch for each other: removing or altering either half is inconsistent authority state and the read **refuses**, rather than resolving the disagreement — the only direction it could ever be resolved in is "usable". | COMPONENT-LOCAL to `createSqliteBoundedGrantStore` | RUNTIME + TEST-ENFORCED | `AUTHORITATIVE_GRANT_STORE.md` §9.1; `revocation-deleted`, `pointer-cleared`, `orphan-revocation` | Closes **partial** deletion. Does **not** close a writer who rewrites both halves consistently — same limit as SEC-INV-036 |
| SEC-INV-038 | The bounded-grant exercise path depends on a **read-only** store port: `issue` and `revoke` are not declared on the interface it is handed, so writing a grant from the exercise path is not expressible. | PATH-LOCAL to bounded-grant exercise | TYPE-LEVEL + STRUCTURAL + TEST-ENFORCED | `BoundedGrantReaderPort` in `grant-store-port.ts`; `grant-execution-service.ts`; `bounded-grant-store-boundaries.test.ts` | Narrows what the exercise path can reach. It does not constrain a host that retains its own store reference (SEC-TRUST-001, D-GS3) |

### 4.5 Server-side execution adapter routing

Alias ids `ADAPTER-REG-0x` are given so these can be cited from the registry's own design document; the canonical id is the `SEC-INV-` number.

| ID | Invariant | Scope | Enforcement | Evidence | Boundary |
|---|---|---|---|---|---|
| SEC-INV-039 (**ADAPTER-REG-01**) | Caller input cannot select an execution adapter. No `adapterId`, `provider`, `destination`, `url`, `host`, `endpoint` or `credential` exists on `GovernedActionIntent`, `KernelEvaluationRequest`, `GrantExerciseRequest` or `ValidatedExecutionAction`, and an intent carrying one is **rejected** rather than sanitized. | PATH-LOCAL to bounded-grant execution | TYPE-LEVEL + RUNTIME + TEST-ENFORCED | `intent.ts` (closed validator, reserved context keys); `execution-adapter-registry.test.ts`; `emergency-control-composition.test.ts` | Routing is host configuration. A caller influences *which* provider runs only by asking for a different action or resource — the same material the Kernel decided on and the grant contained. |
| SEC-INV-040 (**ADAPTER-REG-02**) | The registry receives only a `ValidatedExecutionAction` — an action that already passed the authoritative grant re-read and the containment assessment — and holds no Kernel, policy evaluator, grant, grant store, Governance Store, clock or HTTP type. | COMPONENT-LOCAL to `createExecutionAdapterRegistry` | STRUCTURAL + TYPE-LEVEL + TEST-ENFORCED | `execution-adapter-registry.ts` (two imports only); `no-bypass-effect-paths.test.ts`; `security-invariants.test.ts` | Routing chooses **where** an authorized action is translated, never **whether** it is authorized. |
| SEC-INV-041 (**ADAPTER-REG-03**) | One routing decision invokes **at most one** registered child, at most once, with the assessed action unchanged. An unknown id, an undefined route or a throwing selector invokes none. | COMPONENT-LOCAL to `createExecutionAdapterRegistry` | RUNTIME + TEST-ENFORCED | `execution-adapter-registry.test.ts` counts child invocations per row and deep-equals the action the child saw against the action a directly-composed adapter saw | An unresolved route is reported as `ADAPTER_ERROR`, never as a Kernel denial and never by falling through to an arbitrary provider. |
| SEC-INV-053 (**ADAPTER-REG-05**) | The durable execution record names the adapter that **performed** the effect — the routed child, not the routing boundary — so two effects through two providers are distinguishable afterwards. The identity is written by trusted routing only, from the child identity **snapshotted at composition** — never the child's live `adapterId`, which it could reassign; a child's own claim is discarded, no caller input reaches it, and a **directly composed** adapter cannot override its own identity either — `GrantExecutionService` reads `result.adapterId` only from an adapter that `isExecutionAdapterRegistry` confirms is a real registry, so an adapter returning someone else's id is recorded under its own. | PATH-LOCAL to the Governed Action path | RUNTIME + TEST-ENFORCED | `execution-adapter-registry.ts` (sets it from frozen membership); `execution-ledger.ts` (`executed@<adapterId>`); `execution-adapter-registry.test.ts`; `emergency-control-governed-action.test.ts` | Recorded as **evidence**, never read to decide anything, and never placed on `GovernedActionResult`. An id that could not be recorded exactly is refused at composition. |
| SEC-INV-042 (**ADAPTER-REG-04**) | Registry membership is immutable during traffic: construct, validate, freeze. There is no `register`/`unregister` surface, duplicate and blank ids are refused at composition, and a registry may not be registered inside a registry. Each child's identity is read once into a registry-owned frozen descriptor, so reassigning a child's `adapterId` after composition changes neither routing, the adapter-scoped emergency query, nor attribution. | COMPONENT-LOCAL to `createExecutionAdapterRegistry` | RUNTIME + TEST-ENFORCED | `ExecutionAdapterRegistryError` cases; `Object.isFrozen` and key-set assertions in `execution-adapter-registry.test.ts` | A composing host that retains a raw child reference can still call it directly (SEC-TRUST-001, SEC-TRUST-004, SEC-TRUST-006). |

### 4.6 Operational emergency control

Alias ids `EMERGENCY-0x` are given so these can be cited from the interlock's own design document; the canonical id is the `SEC-INV-` number. All of these hold **only** when a deployment composes `emergencyControl`; unconfigured, no check runs and behaviour is byte-identical to the capability not existing (SEC-INV-006).

| ID | Invariant | Scope | Enforcement | Evidence | Boundary |
|---|---|---|---|---|---|
| SEC-INV-043 (**EMERGENCY-01**) | An active applicable emergency control prevents new governed execution: the action is withheld, no new bounded grant is minted, and no adapter is invoked. | PATH-LOCAL to the Governed Action / bounded-grant path | RUNTIME + TEST-ENFORCED | `orchestrator.ts` admission checkpoint; `emergency-control-governed-action.test.ts` | Does **not** apply to `AocKernel.enforce()`, Sovereign Access, Content Protection, or any code calling a provider directly. |
| SEC-INV-044 (**EMERGENCY-02**) | Unreadable emergency state fails closed. A reader that throws, returns a non-assessment, returns a promise, is closed, or holds state that fails validation yields `unavailable`, and `unavailable` withholds. | PATH-LOCAL to the Governed Action / bounded-grant path | RUNTIME + TEST-ENFORCED | `readEmergencyControl`; `emergency-control-reader.test.ts`; `emergency-control-durability.test.ts` | An outage is never permission. |
| SEC-INV-045 (**EMERGENCY-03**) | Emergency control is re-read at the bounded-grant **commit boundary**, so a stop activated after admission and before the commit refuses the grant. | PATH-LOCAL to authority-controlled issuance | RUNTIME + TEST-ENFORCED | `issuance-core.ts` inside the store's `commitGuard`; `emergency-control-commit-boundary.test.ts` proves the read lies between the guard's entry and exit | A pre-issuance check alone is insufficient, and is not what is implemented. |
| SEC-INV-046 (**EMERGENCY-04**) | Emergency control is re-checked **after** the authoritative grant re-read and containment assessment and **before** provider execution, so a grant that remains valid cannot execute during a stop. | PATH-LOCAL to bounded-grant exercise | RUNTIME + TEST-ENFORCED | `grant-execution-service.ts`; `execution-emergency-control.test.ts` (adapter invocation count `0` on every stopped row) | The grant gate answers first: an expired or revoked grant is still reported in the grant-exercise vocabulary. |
| SEC-INV-047 (**EMERGENCY-05**) | An adapter-scoped stop is enforced after trusted routing and before the child adapter is invoked, and is never reported as a provider rejection. | COMPONENT-LOCAL to `createExecutionAdapterRegistry` | RUNTIME + TEST-ENFORCED | `execution-adapter-registry.ts`; `execution-adapter-registry.test.ts`; `emergency-control-governed-action.test.ts` | The **type is not provenance**. `EmergencyControlWithheldError` has an ordinary constructor, so a directly composed adapter could throw a genuine instance and relabel its own provider failure as an operator stop. `GrantExecutionService` honours the signal only when `isExecutionAdapterRegistry(adapter)` confirms the composed adapter came from `createExecutionAdapterRegistry`, because only a registry consults a reader before reaching a child. Every other throw — plain, lookalike, or a real instance from a direct adapter — stays `ADAPTER_ERROR`. Nothing a **child** controls can raise it through the registry either: the child call *and every read of its returned result* sit inside one catch, through `readExecutionAdapterResult`, so a result getter or Proxy trap throwing a genuine instance is `ADAPTER_ERROR`; the service reads any adapter's result the same way, after the signal check. |
| SEC-INV-048 (**EMERGENCY-06**) | Emergency control never creates or modifies a Kernel decision status or reason code. It produces no `allowed`, `denied`, `indeterminate` or `approval_required`, and the committed Governance Record is unchanged by it. | LAYER-LOCAL to the emergency-control runtime | STRUCTURAL + TEST-ENFORCED | `emergency-control-boundaries.test.ts` bans every decision shape from the module; `emergency-control-governed-action.test.ts` reads the committed record back | SEC-INV-001 is unaffected: the Kernel remains the only decision producer. |
| SEC-INV-049 (**EMERGENCY-07**) | Emergency control does not revoke a bounded grant: no `GrantRevocation` is written, the grant store is not mutated, and releasing the control restores execution under the untouched grant. | PATH-LOCAL to the Governed Action / bounded-grant path | STRUCTURAL + RUNTIME + TEST-ENFORCED | the module imports no grant runtime and names no `revoke` (`emergency-control-boundaries.test.ts`); store reads after a stop in `execution-emergency-control.test.ts` and `emergency-control-governed-action.test.ts` | An interlock is reversible by an operator; a revocation is not. |
| SEC-INV-050 (**EMERGENCY-08**) | Current emergency state never rewrites a recorded historical execution outcome. A replay reports the layer that actually withheld, the failure that actually occurred, or the effect that actually happened. | PATH-LOCAL to the Governed Action path | RUNTIME + TEST-ENFORCED | replay ordering in `orchestrator.ts` (replay precedes every mutable gate); layered ledger encoding in `execution-ledger.ts`; the replay suite in `emergency-control-governed-action.test.ts` | The ledger's only behavioural use stays negative: an attempted execution identity is not attempted again. |
| SEC-INV-051 (**EMERGENCY-09**) | Execution and authorization components depend only on `EmergencyControlReaderPort`, never on the operator writer. The reader declares no mutation method, so disabling the interlock from the path it governs is not expressible. | PATH-LOCAL to the Governed Action / bounded-grant path | TYPE-LEVEL + STRUCTURAL + TEST-ENFORCED | `EmergencyControlReaderPort`; `createEmergencyControlReader` hands the execution path a fresh one-method object rather than the store, so the narrowing survives a cast; `emergency-control-boundaries.test.ts`; `emergency-control-composition.test.ts` asserts no execution source names the store port or calls `activate`/`release` | Does not constrain a host that retains its own store reference (SEC-TRUST-001). |
| SEC-INV-054 (**EMERGENCY-11**) | An active emergency control cannot be removed silently. The durable store keeps an append-only, sequenced, chained event history, a current-state projection pointing at the event that produced it, and a head row anchoring the latest sequence, count and digest; a read cross-checks all three **and walks the whole chain** — every event contiguous from genesis, every digest recomputed, every `previousEventDigest` link checked — so a deleted control row, a deleted event, a deleted head, a re-pointed or stale projection, and a rewritten *older* event each report `unavailable`. `active()` and `health()` reconcile every key known to the event history or the projection before reporting, so a deleted active projection or an `active` flag flipped to `0` cannot leave diagnostics claiming health. A release is an explicit recorded transition; a row that vanishes is damage, never consent. | COMPONENT-LOCAL to `createSqliteEmergencyControlStore` | RUNTIME + TEST-ENFORCED | `emergency-control-record.ts`; `sqlite-emergency-control-store.ts` (`verifiedHead` walks the chain, `verifiedControl`, `runActive` enumerates known keys); the deletion, chain-corruption and operator-diagnostic regressions in `emergency-control-durability.test.ts` | **Unkeyed digests.** Detects partial deletion and mutation; does not stop a writer who re-seals every record consistently, and cannot detect wholesale replacement of the database file (GS-002, SEC-TRUST-002). The chain walk is **O(number of operator transitions)** per read — bounded by operator actions, never by request traffic — and is deliberately uncached, so it cannot go stale against the commit boundary. |
| SEC-INV-052 (**EMERGENCY-10**) | The commit-boundary emergency read is **synchronous**: the port returns an assessment rather than a promise, the whole emergency-control module contains no `await` or `async`, and the commit-boundary path contains no promise, network call, filesystem call or store read. | TYPE-LEVEL + PATH-LOCAL to authority-controlled issuance | TYPE-LEVEL + STRUCTURAL + TEST-ENFORCED | `emergency-control-port.ts`; `emergency-control-commit-boundary.test.ts` | A suspension there reopens the TOCTOU window the guard exists to close. This is why the durable implementation is `better-sqlite3`. |

### 4.7 Customer governed-action API (P5)

Alias ids `GOV-API-0x` are given so these can be cited from the API documents; the canonical id is the `SEC-INV-` number. All of these hold **only** when a deployment composes both `customerIdentityAdmission` and `governedActionOrchestrator` — unconfigured, the route does not exist (SEC-INV-056) and there is nothing to hold. `POST /api/governed-actions` is the **first** customer-facing HTTP route onto the bounded-grant path; it does not make any other effect path governed (NO_BYPASS §5.11).

| ID | Invariant | Scope | Enforcement | Evidence | Boundary |
|---|---|---|---|---|---|
| SEC-INV-055 (**GOV-API-01**) | **Governed-action HTTP identity.** No actor, organization, principal, external subject or system context comes from the request. They come only from `CustomerIdentityAdmission`: the configured credential's non-secret `customerIdentity` + `organizationId`, and the Kernel Authority subject binding. A body that names any of them is **rejected**, not sanitized. | PATH-LOCAL to `POST /api/governed-actions` | TYPE-LEVEL + RUNTIME + TEST-ENFORCED | `EnterpriseGovernedActionContext` carries only `authorizationHeader`; `govern-governed-action-request.ts` hands `govern()` only `admitted.identity`; closed `validateGovernedActionIntent`; `governed-action-api-endpoint.test.ts` (identity rows 12–15, including the persisted Kernel request's actor and organization) | Identity is exactly as strong as the configured bearer API key and the operator-provisioned binding (SEC-TRUST-001, SEC-TRUST-007). No OIDC, mTLS or workload identity. |
| SEC-INV-056 (**GOV-API-02**) | **Route composition.** `POST /api/governed-actions` is mounted only when **both** customer identity admission and governed-action orchestration are composed. Either one alone — or neither — yields the ordinary `404 NOT_FOUND`; there is no fallback identity, no legacy-auth fallback and no weaker implementation. | COMPONENT-LOCAL to the Enterprise HTTP adapter and the composition root | STRUCTURAL + RUNTIME + TEST-ENFORCED | the two-capability condition in `node-http-adapter.ts`; `AocEnterprise.governAction` composed only when both exist; `scripts/check-api-freeze.mjs` proves the route **unmounted** on the default Host (`capabilityGatedProbes`); mounting rows 1–4 and 3b in `governed-action-api-endpoint.test.ts`; `customer-identity-composition.test.ts` IDENTITY-12 | A host that writes its own listener around `AocEnterprise` is trusted code and can expose `governAction` however it likes (SEC-TRUST-001). |
| SEC-INV-057 (**GOV-API-03**) | **No customer authority artifact.** No HTTP response on this route and no SDK type contains a bounded grant, grant id, grant digest, grant scope, authority binding, credential, adapter identity or system context. | PATH-LOCAL to `POST /api/governed-actions`; COMPONENT-LOCAL to `packages/enterprise-host-sdk` | TYPE-LEVEL + TEST-ENFORCED | `GovernedActionResult` (server) and its SDK mirror declare none of them; `governed-action-api-endpoint.test.ts` walks every key of every response across executed, withheld, denied, rejected, failed and refused outcomes, and scans for every credential and the routed adapter id | `providerRef` is passed through as the adapter reported it. A trusted adapter decides what it contains (SEC-TRUST-006). |
| SEC-INV-058 (**GOV-API-04**) | **Routing remains server-side.** Caller-controlled JSON cannot select an adapter, provider, URL, host, origin, destination, endpoint, credential, provider payload or signer: each is rejected at the top level and, where reserved, inside `assertedContext`. Extends SEC-INV-039 to the HTTP boundary. | PATH-LOCAL to `POST /api/governed-actions` | TYPE-LEVEL + RUNTIME + TEST-ENFORCED | closed validator and `GOVERNED_ACTION_RESERVED_CONTEXT_KEYS`; routing rows 25–28 (adapter invocation count `0`, Kernel count `0`); the SDK's `GovernedActionIntent` is closed and `@ts-expect-error`-tested | As SEC-INV-039: a caller influences which child runs only through `action`/`resource`, the material the Kernel decided on and the grant contained. |
| SEC-INV-059 (**GOV-API-05**) | **One canonical execution path.** The endpoint reaches an `ExecutionAdapter` only through `GovernedActionOrchestrator.govern()` → committed Governance Record → ACE issuance → ACE exercise → (registry) → child. The HTTP adapter and the application sequence call no Kernel, Governance Store, grant store, issuance, exercise, registry or adapter themselves. | PATH-LOCAL to `POST /api/governed-actions` | STRUCTURAL + TEST-ENFORCED | GOV-ACT-11 scans in `governed-action-composition.test.ts` (adapter source; the sequence's four-import allow-list); the two enumerated adapter call sites in `no-bypass-effect-paths.test.ts` are unchanged; adapter invocation counts on every endpoint row | Path-local. Says nothing about `enforce()`, Sovereign Access, Content Protection or direct provider calls (SEC-INV-010, SEC-INV-019, SEC-TRUST-004). |
| SEC-INV-060 (**GOV-API-06**) | **Authentication independent of the legacy toggle.** The route always requires customer-plane admission; `AOC_ENTERPRISE_REQUIRE_AUTH=false` does not make it unauthenticated. A missing, malformed or unknown credential, a legacy or unscoped key, an unbound subject, a revoked actor and an unreadable binding all stop **before** the Kernel. | PATH-LOCAL to `POST /api/governed-actions` | RUNTIME + TEST-ENFORCED | `authenticateCustomerCredential` reads no flag; the sequence names no `requireAuthentication` (structural scan); authentication rows 5–11 run on a Host with the flag **off** and assert Kernel, Governance Store and provider counts of `0` | Transport security (TLS) and credential storage/rotation are the operator's (SEC-TRUST-007). |
| SEC-INV-061 (**GOV-API-07**) | **Emergency administration remains operator-only.** The customer API can observe a withholding result (`withheld` / `emergency-control`, with `EMERGENCY_CONTROL_ACTIVE` or `_UNAVAILABLE`), and cannot activate, release, list or inspect a control, or name one in a request. | PATH-LOCAL to `POST /api/governed-actions`; COMPONENT-LOCAL to the SDK | STRUCTURAL + TEST-ENFORCED | no route, SDK method or intent field (`emergency-control-composition.test.ts`); `governed-action-api-endpoint.test.ts` probes the route's other methods and plausible admin paths and asserts the store is unchanged | A caller learns *that* an applicable stop exists, not which, who declared it, or when. |

### 4.8 Generic HTTP Execution Adapter (P6)

Alias ids `GEN-HTTP-0x` are given so these can be cited from `docs/enterprise/AOC_GENERIC_HTTP_EXECUTION_ADAPTER.md`; the canonical id is the `SEC-INV-` number. Every one of these is scoped to **the Generic HTTP adapter** — a child a deployment composes through `executionAdapterRouting.genericHttpAdapters` — and none is claimed for arbitrary `ExecutionAdapter` implementations, which remain trusted host code (SEC-TRUST-006). Unconfigured, the adapter does not exist and there is nothing to hold. None of this is network egress control: SEC-INV-U03 stays unimplemented.

| ID | Invariant | Scope | Enforcement | Evidence | Boundary |
|---|---|---|---|---|---|
| SEC-INV-062 (**GEN-HTTP-01**) | **The caller describes the action, never the network effect.** No destination, origin, host, method, path structure, header, credential or provider payload can be supplied by a caller: none exists on `GovernedActionIntent`, `KernelEvaluationRequest`, `GrantExerciseRequest` or `ValidatedExecutionAction`, and every one is operator configuration snapshotted at composition. | COMPONENT-LOCAL to the Generic HTTP adapter; PATH-LOCAL to the governed-action path | TYPE-LEVEL + STRUCTURAL + RUNTIME + TEST-ENFORCED | closed intent validator (unchanged); `generic-http-composition.test.ts` J rows (`url`, `origin`, `host`, `endpoint`, `method`, `headers`, `credential`, `providerPayload`, `adapterId`, `provider` → `rejected`, 0 provider calls, 0 DNS lookups, 0 sockets, 0 HTTP requests); §29 option-type scan | A caller still influences *which* pinned integration runs through `action`/`resource`, exactly as SEC-INV-039 states. |
| SEC-INV-063 (**GEN-HTTP-02**) | **One instance, one pinned HTTPS origin.** The origin is `https:` + DNS hostname + optional port and nothing else; userinfo, path, query, fragment, IP literals, `localhost`/`*.localhost`, local-only suffixes, single labels and trailing dots are refused at startup. The hostname is normalized once; the `Host` header and TLS SNI come from it; no request value can alter it. | COMPONENT-LOCAL to the Generic HTTP adapter | STRUCTURAL + RUNTIME + TEST-ENFORCED | `configuration.ts` `snapshotOrigin`; origin matrix rows 1–12 in `generic-http-execution-adapter.test.ts`; `buildNodeRequestOptions` (`servername`, `setHost`, no `host`); real-socket SNI capture | The operator chooses the origin. A malicious public provider can still forward the request elsewhere. |
| SEC-INV-064 (**GEN-HTTP-03**) | **Resolve, judge every answer, bind the socket.** Every execution resolves the pinned hostname afresh, rejects the **whole** answer if any address is not publicly routable (special-purpose IPv4 ranges including `169.254.169.254`; IPv6 outside `2000::/3` or in reserved blocks; every IPv4-mapped, compatible, NAT64, 6to4 and Teredo form; anything unparseable), and hands the one approved address to `https.request` through a `lookup` that ignores the hostname — so no second resolution exists to rebind. The TCP peer is checked against it before TLS. | COMPONENT-LOCAL to the Generic HTTP adapter | RUNTIME + TEST-ENFORCED | `public-address-policy.ts`; `selectApprovedAddress`; address matrices and rebinding rows 32–34; real-socket test dialling the approved IP for an unresolvable hostname | Application-level only. A resolver answer naming a public address that the operator did not intend is not detectable here. |
| SEC-INV-065 (**GEN-HTTP-04**) | **No second or unpinned destination.** No redirect is followed (every 3xx is `unconfirmed`, never a definite failure, and `Location` is never read), no proxy is consulted (`agent: false`; no proxy, agent, dispatcher or lookup option exists), nothing is retried, and no connection is reused across executions. | COMPONENT-LOCAL to the Generic HTTP adapter | STRUCTURAL + RUNTIME + TEST-ENFORCED | redirect rows 2–6 and 15 (one request, one resolution), proxy-environment row 17 over a real TLS server, the closed-contract refusal of `followRedirects`/`proxy`/`agent`/`lookup`/…, transport source scan | Node's own networking stack is trusted. |
| SEC-INV-066 (**GEN-HTTP-05**) | **Outbound values come only from approved mapping sources.** Every outbound path segment, query value, header value and body field is an operator literal or exactly one of `subject`, `action`, `resource`, `counterparty`, `organization`, `amount.value`, `amount.unit`, `notAfter`, `correlation.*`. `boundedGrantId` and `assertedContext` are not sources; a missing required source stops the request before any I/O; path values are single encoded segments. | COMPONENT-LOCAL to the Generic HTTP adapter | TYPE-LEVEL + RUNTIME + TEST-ENFORCED | `GENERIC_HTTP_ACTION_SOURCES`, null-prototype reader table; mapping matrix rows 1–31; §44 integration asserting the provider request carries no grant id and no asserted-context value | The values are the ones the grant covered; what the provider does with them is the provider's. |
| SEC-INV-067 (**GEN-HTTP-06**) | **Credentials are operator configuration only.** A bearer token or one explicit credential header, snapshotted at composition, injected by the adapter, and — as a **literal** value — never present in a URL, body, `providerRef`, result `detail`, `GovernedActionResult`, Governance Record, execution ledger, log, event, health report or configuration error. `Authorization`, `Cookie` and proxy headers cannot be set through ordinary header mapping. `providerRef` is treated as untrusted provider-controlled input: a candidate containing any configured secret (bearer token, `Bearer <token>`, or the header credential value — exact, case-sensitive substring) is omitted outright, so a provider cannot reflect the credential back through it; a header credential value must be a canonical visible-ASCII opaque token with no space or tab (refused, never trimmed), so HTTP OWS normalization cannot change the effective credential away from the value the filter protects; `providerRefHeader` may not name the credential header or any authentication/credential-bearing header (`Authorization`, `Proxy-Authorization`, `WWW-Authenticate`, `Proxy-Authenticate`, `Cookie`, `Set-Cookie`). | COMPONENT-LOCAL to the Generic HTTP adapter | RUNTIME + TEST-ENFORCED | credential rows 18–20; the fixed-phrase detail row; reflected-secret rows (exact, embedded and `Bearer <token>` reflections omitted, outcome unchanged, one request) and the governed-action reflection row; composition J row searching result, record, logs, events and health for the sentinel | The secret lives in process memory. No KMS, HSM, OAuth, refresh or mTLS (SEC-INV-U02 unchanged). Literal non-disclosure only: HTTP's own whitespace normalization is covered by the no-whitespace rule, but a provider that otherwise transforms or encodes the credential before reflecting it is not detected. |
| SEC-INV-068 (**GEN-HTTP-07**) | **Uncertainty is never reported as a definite failure.** Once the TLS session exists, any loss of certainty before a final status (timeout, reset, close, stream error), and every final status other than the Stage-A definitive successes 200, 201, 204 and an ordinary 4xx other than 408 — i.e. every other 2xx (202 Accepted included), every 3xx, 408, every 5xx and anything unclassifiable — is `unconfirmed` → `execution-unconfirmed` → `execution_unconfirmed` with `GOVERNED_ACTION_EXECUTION_OUTCOME_UNCONFIRMED`, durably recorded as `execution-unconfirmed@<adapterId>` and replayed without another invocation. `PROVIDER_UNAVAILABLE` is reserved for failures proven to precede transmission. | COMPONENT-LOCAL to the Generic HTTP adapter for the classification; PATH-LOCAL to the governed-action path for recording and replay | RUNTIME + TEST-ENFORCED | status matrix; fake-primitive phase rows; real-socket reset/timeout rows; orchestrator P6 ledger rows (canonical row, replay, tampered forms, crash-vs-recorded distinction) | Not reconciliation, and not exactly-once: nothing resolves an unconfirmed effect. |
| SEC-INV-069 (**GEN-HTTP-08**) | **At most one outbound request per `execute()`.** The adapter core calls its network runtime's `send` once, in straight-line code with no loop; the transport invokes its request primitive once; a DNS failure, refusal, timeout, reset, redirect or any status produces no second attempt, and there is no fallback to a second resolved address. | COMPONENT-LOCAL to the Generic HTTP adapter | STRUCTURAL + RUNTIME + TEST-ENFORCED | invocation-count rows for every failure class; source scans for exactly one `runtime.send(` and one `primitive(`; the write-ahead claim (unchanged) prevents a second `execute()` for the same execution identity | Provider-side idempotency is the provider's; an operator may map `correlation.executionId` into an idempotency header. |

### 4.9 Aggregate / velocity exercise controls (P7)

Alias ids `EXERCISE-CTRL-0x` / `EXERCISE-CTRL-10` are given so these can be cited from `docs/enterprise/AOC_EXERCISE_CONTROLS.md` and `docs/architecture/ADR-EXERCISE-AGGREGATE-CONTROLS.md`; the canonical id is the `SEC-INV-` number. Every one of these holds **only** when a deployment composes `authorityControlledExecution.exerciseControls`, and **only** on the bounded-grant exercise path — unconfigured, nothing runs and behaviour is byte-identical (SEC-INV-006). None of them is claimed for `AocKernel.enforce`, Sovereign Access, Content Protection, the Pinata or Stripe paths outside ACE, or arbitrary application networking. Each narrows; none can widen a decision, a grant or an attempt.

| ID | Invariant | Scope | Enforcement | Evidence | Boundary |
|---|---|---|---|---|---|
| SEC-INV-070 (**EXERCISE-CTRL-01**) | **No provider execution without a successful reservation.** With exercise controls composed, `GrantExecutionService` invokes the adapter only after `ExerciseControlGate.admit()` returned `admitted` — binding #1, a valid policy snapshot and an atomic reservation — and then, in order, a second authoritative grant read and containment assessment at a fresh injected-clock instant, binding #2 against that read (`ExerciseControlGate.revalidate()`, synchronous) and the emergency re-check. Nothing awaited sits between the last of them and the adapter; the validated action is built from the second read. | PATH-LOCAL to the P7-enabled bounded-grant exercise path | STRUCTURAL + RUNTIME + TEST-ENFORCED | `grant-execution-service.ts` ordering (`execution-layer-boundaries.test.ts` P7 describe: admit → grant read #2 → revalidate → re-check → `adapter.execute`, no `await` after read #2; `execution-exercise-control.test.ts` asserts the checkpoint order and the expiry / revocation / store-failure / binding / emergency rows after the reservation); `execution-exercise-control.test.ts` counts adapter invocations `0` on every refusal row | The adapter is trusted host code once invoked (SEC-TRUST-006). |
| SEC-INV-071 (**EXERCISE-CTRL-02**) | **Admission is atomic across every applicable limit.** Sampling the reservation instant, reading active usage, testing every limit and inserting the reservation happen in one critical section — one `BEGIN IMMEDIATE` SQLite transaction — so every limit is admitted together or none is, and processes sharing one file cannot both win the last unit. | COMPONENT-LOCAL to `ExerciseControlLedgerPort` implementations | RUNTIME + TEST-ENFORCED | `runReserve.immediate(...)` in `sqlite-exercise-control-ledger.ts`; the all-or-nothing contract rows; `exercise-control-concurrency.test.ts` (two connections; worker-thread races of 2, 24 and 10 participants, repeated) | One SQLite file on one host. Separate files on separate hosts do not share a quota; SQLite on a network filesystem that does not honour its locking is not a distributed lock. |
| SEC-INV-072 (**EXERCISE-CTRL-03**) | **A pending reservation consumes, from its admission instant.** A reservation with no terminal event counts toward every bucket it names from the moment it commits; nothing expires, sweeps or releases it, including at startup. The reservation instant is assigned by the ledger from its injected clock inside the admission critical section — after the SQLite write lock is held — and is the one instant used for the rolling-window threshold, every persisted copy and the returned record; no caller supplies it. | COMPONENT-LOCAL to the exercise-control ledgers | RUNTIME + TEST-ENFORCED | contract rows §40.5, §41.8; crash-conservatism rows in `exercise-control-sqlite.test.ts`; `exercise-control-boundaries.test.ts` bans timers, sweepers and TTLs | Availability loss after a crash is accepted and conservative; reconciliation is future work. |
| SEC-INV-073 (**EXERCISE-CTRL-04**) | **An unconfirmed execution consumes.** `execution-unconfirmed` settles — it never releases — so an effect the provider may have performed never returns its capacity. | PATH-LOCAL to the P7-enabled bounded-grant exercise path | STRUCTURAL + RUNTIME + TEST-ENFORCED | `reservationDispositionFor` (exhaustive, `never`-closed); Generic HTTP 202/302/408/500 rows in `exercise-control-governed-action.test.ts`; amount row §42.16 | Not reconciliation, not exactly-once. |
| SEC-INV-074 (**EXERCISE-CTRL-05**) | **Only a definite no-effect result, or a withholding after the reservation, releases.** `execution-failed` (including an adapter throw and a malformed result), the post-reservation grant re-read and re-assessment (`grant-exercise`), the binding re-check, the emergency re-check and an adapter-scoped stop release; a release or settlement that cannot be recorded leaves the reservation consuming and never rewrites the provider outcome. | PATH-LOCAL to the P7-enabled bounded-grant exercise path | STRUCTURAL + RUNTIME + TEST-ENFORCED | the single `finish(...)` exit (structural scan: every post-reservation `return` is `return finish(`); §45 rows 1–16 in `execution-exercise-control.test.ts` | `ADAPTER_ERROR` from a throwing adapter is released on the port's own contract; an adapter that performs an effect and then throws is trusted host code misreporting it. |
| SEC-INV-075 (**EXERCISE-CTRL-06**) | **An unreadable, unavailable or corrupt ledger fails closed.** A ledger that throws, answers outside its contract, is closed, or holds state failing any validation — schema, record, rule, terminal-event or bucket-head digest, rule count, ordinals, instant, id derivation, policy digest — yields `EXERCISE_CONTROL_LEDGER_UNAVAILABLE`; nothing is repaired. Every reservation a bucket names is verified before the rolling window is applied to its verified instant, so no unverified field can exclude its own row from verification. An unknown schema version is refused at open before mutation. | PATH-LOCAL to the P7-enabled path; COMPONENT-LOCAL to `createSqliteExerciseControlLedger` for validation | RUNTIME + TEST-ENFORCED | corruption, schema, closure and health rows in `exercise-control-sqlite.test.ts`; gate rows for throwing and out-of-contract ledgers | **Unkeyed digests.** Integrity detection, not authenticity: a writer able to rewrite rows and re-seal every relevant digest consistently, or delete whole reservations together with a re-sealed bucket head, is trusted (SEC-TRUST-002). A single field edited without re-sealing — including a rule row's timestamp moved backwards out of a rolling window — fails closed. |
| SEC-INV-076 (**EXERCISE-CTRL-07**) | **The authority binding is revalidated against immutable grant provenance before provider execution.** The grant carries `authorityBindingDigest` (canonical SHA-256 of the binding resolved and commit-proved at issuance); at exercise, the current binding's digest must equal it **exactly**, before the reservation and again after it, against the grant as re-read from the authoritative store. A grant without provenance is unverifiable and withheld. | PATH-LOCAL to the P7-enabled bounded-grant exercise path | TYPE-LEVEL + RUNTIME + TEST-ENFORCED | `verifyExerciseAuthorityBinding`; `exerciseAuthorityBindingDigestResolver`; `exercise-control-authority-binding.test.ts` §47 rows (authorityRef, horizon, kind, justification, unverifiable forms, legacy grants); binding-change-during-reservation row | **Not atomic with the external authority store or the provider**: a grant, binding or emergency control can change after the final check; no local check is atomic with the provider. The resolver is trusted host code. The issuance commit-boundary equality check (SEC-INV-014 area) is unchanged. |
| SEC-INV-077 (**EXERCISE-CTRL-08**) | **One execution identity cannot invoke an adapter twice** through the P7-enabled path. The reservation id is derived from the grant and the execution id, execution ids are unique in the ledger, and a re-delivery is `EXECUTION_ALREADY_RESERVED` (or `RESERVATION_CONFLICT`) with no adapter call. | PATH-LOCAL to the P7-enabled bounded-grant exercise path | RUNTIME + TEST-ENFORCED | contract §44 rows; `execution-exercise-control.test.ts` duplicate rows; the unique `execution_id` index; the same-identity worker race | The Governed Action write-ahead claim is unchanged and independent (SEC-INV-050 area). |
| SEC-INV-078 (**EXERCISE-CTRL-09**) | **A caller cannot choose a limit, bucket, policy, reservation or binding.** None exists on `GovernedActionIntent`, `KernelEvaluationRequest`, `GrantExerciseRequest` or `ValidatedExecutionAction`; P7 names are undeclared intent fields and reserved `assertedContext` keys; the policy and resolver queries are built only from the authoritative grant and contained attempt fields. | PATH-LOCAL to `POST /api/governed-actions` and the bounded-grant path | TYPE-LEVEL + STRUCTURAL + RUNTIME + TEST-ENFORCED | closed intent validator + reserved context keys; `governed-action-api-endpoint.test.ts` P7 rows; §50 rows in `exercise-control-governed-action.test.ts` (0 Kernel commits, 0 grant issuance, 0 policy, 0 ledger, 0 DNS, 0 sockets); query-shape structural rows | The host policy and resolver are trusted; a malicious host policy is a malicious host. |
| SEC-INV-079 (**EXERCISE-CTRL-10**) | **Amount aggregation is exact decimal arithmetic in exact units.** Maxima are canonical decimal text; an attempt is converted once from its JavaScript number; every sum is `BigInt` coefficient/scale arithmetic; units are compared exactly and never converted; no JavaScript float accumulation and no SQL aggregate over a floating column exists on the path. | COMPONENT-LOCAL to the exercise-control runtime and ledgers | STRUCTURAL + RUNTIME + TEST-ENFORCED | `exercise-decimal.ts`; §42 rows (0.1 + 0.2 under 0.3, 1e-7, large finite, unit mismatch); source scans for `parseFloat`, `Math.round`, `SUM(`, `REAL` | Input precision is bounded by the caller's JSON / JavaScript number. No FX. |

### 4.10 Canonical authority event stream (P8, Stage A)

Alias ids `EVENT-STREAM-0x` / `EVENT-STREAM-09` are given so these can be cited from `docs/enterprise/AOC_CANONICAL_AUTHORITY_EVENT_STREAM.md` and `docs/architecture/ADR-CANONICAL-AUTHORITY-EVENT-STREAM.md`; the canonical id is the `SEC-INV-` number. Every one of these holds **only** when governed actions are composed — the stream is composed with the Governed Action Orchestrator and absent otherwise — and **only** for the governed-action / bounded-grant lifecycle it projects (Stage A is path-local). The stream is **evidence**: none of these invariants makes it authoritative for anything, and the first two exist to prove it cannot become so.

| ID | Invariant | Scope | Enforcement | Evidence | Boundary |
|---|---|---|---|---|---|
| SEC-INV-080 (**EVENT-STREAM-01**) | **Evidence never becomes authority.** No Kernel, policy, grant, exercise, exercise-control, emergency-control, routing, adapter, customer-identity, Governance Store or replay module imports the stream's store, reader, verifier, projector or health. The one name lifecycle code may use is the write-only `AuthorityEventRecorder`, **type-only**, in exactly two files (the orchestrator and ACE); the exercise-control gate reaches it only through its own write-only `ExerciseControlObserver` port. Every recorder/observer method returns **`void`**, and every call site reports inside a catch-all and never assigns, compares or branches on the call. | PATH-LOCAL to the governed-action / bounded-grant lifecycle | TYPE-LEVEL + STRUCTURAL + TEST-ENFORCED | `authority-event-stream-boundaries.test.ts` (import allow-list over every authority-bearing source, holder allow-list over all of `src/`, `void`-return scan, discard-pattern scans at all three call sites, Kernel/feature runtimes name no stream symbol) | A future module outside the scanned directories is covered by the all-of-`src/` holder scan, not by the import allow-list. |
| SEC-INV-081 (**EVENT-STREAM-02**) | **A projection failure never changes an outcome.** A store that throws, is closed or cannot be opened, a conflicting or invalid fact, a corrupt stream, or a recorder that throws leaves every decision, grant issuance, revocation, reservation, settlement, release, routing choice, adapter invocation count and `GovernedActionResult` byte-identical to a deployment with no stream; it is counted in the `aoc.enterprise.authority-event-stream` module's health (`degraded` / `unhealthy`), which is `optional`, never throws at initialization, and cannot take the Host out of `ready`. | PATH-LOCAL to the governed-action / bounded-grant lifecycle | RUNTIME + TEST-ENFORCED | §31 three-way rows in `authority-event-stream-governed-action.test.ts` (no stream vs failing store vs throwing recorder: results, adapter calls, grant issuance, Governance Store references, ledger state and revocations identical); composition rows for an unopenable and a failing store | Evidence written by a failed projection is lost, not retried; the stream can be shorter than what happened, never longer. |
| SEC-INV-088 (**EVENT-STREAM-09**) | **Durable event projection is not part of authority control flow.** Reporting a fact **enqueues** it and returns: recorder and observer methods return `void`, no authority-bearing source awaits one, and even the first append of an idle stream starts a microtask later, so no store work runs inside the reporting call's own stack frame. An **asynchronous** append that is slow, unreachable or **never settles** therefore cannot hold a committed decision before its grant, sit between the durable write-ahead claim and the adapter crossing, keep a P7 reservation consuming while admission never returns, or withhold a revocation's confirmation. Per-stream serial chains preserve append order without any caller waiting, and a grant's intake chain keeps a revocation whose lifecycle must be resolved asynchronously from being overtaken by a later fact of the same lifecycle; the store still assigns sequence, previous digest, `recordedAt` and digest. | PATH-LOCAL to the governed-action / bounded-grant lifecycle | TYPE-LEVEL + STRUCTURAL + RUNTIME + TEST-ENFORCED | never-settling rows A–E in `authority-event-stream-governed-action.test.ts` (decision, claim, reservation, revocation, outcome — outcomes, adapter counts, Governance Store references, ledger state and replay unchanged); `authority-event-stream-projection.test.ts` (enqueue returns before any append is invoked; one-at-a-time per stream in enqueue order; slow revocation attribution overtaken by nothing; a stuck stream or grant holds only itself); `exercise-control-observer.test.ts` never-settling rows; `await`-ban scans in `authority-event-stream-boundaries.test.ts` | **Logical control-flow decoupling, not latency or thread isolation.** Projection runs in the same process and on the same event loop: the built-in SQLite store is `better-sqlite3`, whose `BEGIN IMMEDIATE` append — lock wait and `fsync` included — is synchronous work on that loop, and a host-supplied store may be synchronous too. So a slow or blocking store **can** add process latency to whatever runs next, including an authority path; what it cannot do is make an authority path wait for projection to complete. A hostile in-process recorder can still block synchronously (an infinite loop) — trusted host code, as every composed port is (SEC-TRUST-006). Queue depth is an operator signal (`pending`), never back-pressure. Worker isolation is not in Stage A. |
| SEC-INV-082 (**EVENT-STREAM-03**) | **Events follow facts.** Each event is emitted only after its fact is established in its authoritative home: the decision after commit **and** re-read + verification; the grant after issuance returned it; the revocation after the store returned it; the claim after the attempt row was appended by that call; reservation facts after the ledger returned them; the outcome after the runtime returned it. No "intent to" event exists; an attempt is named as an attempt; expiry is an observation keyed by the grant at its own `expiresAt`, never a scheduled or persisted transition. | PATH-LOCAL to the governed-action / bounded-grant lifecycle | STRUCTURAL + RUNTIME + TEST-ENFORCED | ordering scans in `authority-event-stream-boundaries.test.ts`; uncommitted-decision rows (Kernel throws, commit fails → no event) and every lifecycle row in `authority-event-stream-governed-action.test.ts` | Stage A projects only persisted lifecycle facts; withholdings before a grant exists leave only the committed decision on the stream. |
| SEC-INV-083 (**EVENT-STREAM-04**) | **Append-only, contiguous and unforked per stream.** Sequence and previous digest are assigned by the store inside one critical section — in SQLite one `BEGIN IMMEDIATE` transaction that loads and verifies the stream, chooses head + 1 and advances the sealed head — with `UNIQUE (stream_id, sequence)` as an independent refusal; triggers refuse `UPDATE`/`DELETE` on events and `DELETE` on heads; no update, delete, repair or cleanup API exists. | COMPONENT-LOCAL to `AuthorityEventStreamStore` implementations | STRUCTURAL + RUNTIME + TEST-ENFORCED | `authority-event-stream-concurrency.test.ts` (two connections; worker-thread races of 2 and 24 unique facts, repeated; chain verified after every race); trigger and API-shape rows | One SQLite file serializes one host. No global or cross-stream order. |
| SEC-INV-084 (**EVENT-STREAM-05**) | **Tamper-evident, verify-first, never repaired.** Each event's digest covers schema version, ids, organization, type, sequence, both instants, references, payload and previous digest; every append and read verifies the whole chain and the sealed head against each other before believing either, and re-derives each event's stream and event id from its own content. Payload, type, instant, organization, sequence, previous-digest, event-digest, reference, event-deletion and head mutations fail verification; nothing is truncated, skipped, repaired or returned as a verified prefix, and no append extends a stream that fails. | COMPONENT-LOCAL to `AuthorityEventStreamStore` implementations | RUNTIME + TEST-ENFORCED | seventeen raw-SQLite corruption rows and the re-sealed-head row in `authority-event-stream-sqlite.test.ts`; §15 rows in `authority-event-stream-domain.test.ts` | **Unkeyed digests: integrity, not authenticity.** A writer who rewrites a whole stream and re-seals its head, or deletes a stream with its head, is not detected from inside the file (SEC-TRUST-002, SEC-INV-U01). A corrupt stream is an evidence failure only. |
| SEC-INV-085 (**EVENT-STREAM-06**) | **Deterministic identity; no silent overwrite; no duplicate lifecycle facts.** Stream ids derive from (organization, server-derived request id); event ids from (stream, type, source artifact id). The same id with a byte-equivalent canonical fact returns the recorded event unchanged; the same id with any other fact is `AUTHORITY_EVENT_CONFLICT` and writes nothing. A governed-action replay adds no event (its committed decision resolves to the existing one), and the stream is never the replay guard. | PATH-LOCAL to the governed-action / bounded-grant lifecycle | RUNTIME + TEST-ENFORCED | contract idempotency/conflict rows; duplicate and conflicting worker-thread races; §34 replay rows | Not exactly-once: a projection that fails is not retried. |
| SEC-INV-086 (**EVENT-STREAM-07**) | **Tenant-confined.** Every store call carries an organization scope with no `system` escape; a stream's organization is fixed by its first event and sealed in its head; append, read and verify under another organization are refused; a stream id forged under another tenant is refused on identity; the projector serves exactly the orchestrator's organization. | COMPONENT-LOCAL to the stream stores, projector and read surface | RUNTIME + TEST-ENFORCED | contract tenancy rows; composition read-surface rows; organization-mutation corruption row | Stage A has no cross-tenant reader by design. |
| SEC-INV-087 (**EVENT-STREAM-08**) | **Closed, bounded, secret-free payloads with execution certainty intact.** Exactly the declared references and payload keys per type; values are opaque server-derived ids, digests, closed vocabularies, canonical instants, recordable adapter ids and a bounded `providerRef`; any value shaped like a bearer or basic credential, JWT, PEM key, URL, cookie or authorization header, or containing a control character, is refused by the store (and a `providerRef` of that shape is omitted by the projector). `executed`, `execution-failed` (with its reason), `execution-unconfirmed` and `withheld` (with its layer and that layer's codes) are kept distinct and the store refuses payloads that blur them. | COMPONENT-LOCAL to the stream contract and projector | STRUCTURAL + RUNTIME + TEST-ENFORCED | closed-contract, unsafe-value and certainty rows in `authority-event-stream-domain.test.ts`; full-Host secret-leak row in `authority-event-stream-composition.test.ts` (customer bearer, provider bearer, literal header, origin, cookie, response body, asserted context — keys and values) | Value-shape refusal is a pattern list, not a proof: an opaque id that happens to embed a secret cannot be recognized. The projector copies only server-derived fields, which is the load-bearing control. |
| SEC-INV-089 (**MONEY-01**) | **Money is exact data, never an IEEE-754 value, on the governed financial-action spine.** From the governed-action boundary to the adapter a monetary quantity is canonical decimal text (`0`, `[1-9][0-9]*`, optional `.[0-9]*[1-9]`; one spelling per quantity): the intent's `amount.value`, the Kernel request's `action.amount`, the grant's `ceiling.limit`, the exercise request's and the P7 query's `amount.value`, the reservation's usage and the `ValidatedExecutionAction.amount.value`. A v1 JSON number on the wire is read from its exact source characters (JSON source-text access), never re-parsed through a double; an already-parsed JavaScript number is refused rather than re-spelled; a policy pack's `amount` threshold is canonical text or the pack cannot be registered, and a non-canonical policy input amount is `invalid_input`; no production function converts a number into monetary text; a grant ceiling or exercise amount that is not canonical text is malformed and refuses; comparisons and sums are `BigInt` coefficient/scale arithmetic in one shared implementation (`src/features/monetary-runtime`). | PATH-LOCAL to the governed-action / bounded-grant lifecycle | STRUCTURAL + RUNTIME + TEST-ENFORCED | `canonical-decimal.test.ts`, `monetary-boundaries.test.ts`, `canonical-monetary-semantics.test.ts` (a value beyond 2^53 end to end, through a reopened SQLite ledger), `exact-monetary-json.test.ts`, `policy-monetary-precision.test.ts`, v1-wire rows in `governed-action-api-endpoint.test.ts` | Exactness at the wire depends on the runtime exposing JSON source text (Node ≥ 22); without it a JSON number stays a number and is refused, never converted. |
| SEC-INV-090 (**MONEY-02**) | **Every amount names an explicit asset, and scale is trusted host metadata.** A unit must resolve in the deployment's frozen asset registry (one identifier ↔ one definition; no aliases; namespaced identifiers such as `xrpl:USD/rIssuer` so tickers never share an identity); the scale is read from the definition only, and no request field can state or override it. An unknown asset is refused. | COMPONENT-LOCAL to the governed-action boundary and the monetary runtime | RUNTIME + TEST-ENFORCED | `monetary-amount.test.ts` (unknown, case-variant and issuer-variant assets; forged `scale`); P9 rows in `governed-action-api-endpoint.test.ts` | The registry is configuration: a deployment that defines an asset with the wrong scale is trusted as configured. |
| SEC-INV-091 (**MONEY-03**) | **No implicit rounding and no implicit FX.** A value with more fractional digits than its asset's scale is refused, never rounded or truncated (`"10.001"` USD never becomes `"10.00"`); the only normalization is dropping trailing fractional zeros, which changes spelling and never quantity. Amounts in different assets are `incomparable` — grant containment, exercise containment and P7 limits all fail closed on a unit mismatch; no rate, conversion or exchange exists in the monetary runtime. | COMPONENT-LOCAL to the monetary runtime and its consumers (grant bound algebra, exercise assessment, P7) | STRUCTURAL + RUNTIME + TEST-ENFORCED | scale-overflow and FX rows in `monetary-amount.test.ts`; `monetary-boundaries.test.ts` (no float, rounding or FX vocabulary in code) | — |
| SEC-INV-092 (**MONEY-04**) | **Financial classification is host-trusted and cannot be supplied or downgraded by a caller.** The class of a governed action is the host `FinancialActionClassifier`'s answer about the action identifier, never an intent field: `financial`/`actionClass`/`classification`/`scale` are undeclared top-level properties and reserved `assertedContext` keys. A financial action must carry exactly one exact, strictly positive amount; a non-financial action may carry none. So omitting the amount cannot downgrade a financial action, and adding one cannot move money under a non-financial action. | PATH-LOCAL to the governed-action / bounded-grant lifecycle | RUNTIME + TEST-ENFORCED | adversarial rows in `canonical-monetary-semantics.test.ts` and `governed-action-api-endpoint.test.ts` | Without `monetary` configuration nothing is financial, so every amount-bearing intent is refused. |
| SEC-INV-093 (**MONEY-05**) | **P7 relies on the trusted class and on exact text.** When exercise controls are composed, the gate requires the same host classifier instance, classifies the **grant's** action (never the attempt's), exposes the class to the trusted policy as `actionClass`, and withholds before any reservation a financial exercise with no amount, a non-financial exercise with one (`EXERCISE_CONTROL_ACTION_CLASS_MISMATCH`), and any amount that is not canonical text (`EXERCISE_CONTROL_AMOUNT_REQUIRED`) — no number conversion exists on the gate path. | PATH-LOCAL to the governed-action / bounded-grant lifecycle | RUNTIME + TEST-ENFORCED | P7 rows in `canonical-monetary-semantics.test.ts`; `exercise-control-boundaries.test.ts` | Narrows only: it can withhold an exercise the grant covered, never permit one. |
| SEC-INV-094 (**PAY-01**) | **The requested amount never defines its own authority ceiling.** The Kernel grant projection states no amount bound; a financial grant's amount ceiling is attached only by the composition, from durable authority (`withGrantAmountCeiling`), and defaults to the authority's ceiling (a trusted issuer may narrow it through `requestedBounds`; broadening is refused). The requested amount is compared against the authority ceiling exactly and never becomes one; a policy threshold never substitutes for either. | PATH-LOCAL to the governed-action / bounded-grant lifecycle | STRUCTURAL + RUNTIME + TEST-ENFORCED | `authority-payment-ceilings-structure.test.ts` (grant adapter reads no `action.amount`); request-25/authority-100 and policy-10000/authority-100 rows in `authority-payment-ceilings.test.ts`; `bounded-grant-scenario.test.ts` P10 amount-semantics table; `kernel-grant-*` rows | — |
| SEC-INV-095 (**PAY-02**) | **A caller cannot create, raise or select monetary authority.** Payment ceilings and spending limits are provisioned only through the operator surface (`system: true` + operator identity, enforced by the store); undeclared intent keys are refused and the P10 vocabulary (`maxAmount`, `max_amount`, `paymentCeiling`, `spendingLimit(s)`, `budget(Id)`, `remaining`, `limitId`, `scopeKey`, `window`, `financialAuthority`, `authorityLimit`, …) is reserved in `assertedContext`; nothing nested in context reaches the resolver; no route, SDK method or host option supplies a financial resolver. | PATH-LOCAL to the governed-action / bounded-grant lifecycle | RUNTIME + TEST-ENFORCED | caller-isolation rows (top-level, asserted-context and nested) in `authority-payment-ceilings.test.ts`; operator-context row in `kernel-authority-monetary-constraints.test.ts` | — |
| SEC-INV-096 (**PAY-03**) | **A financial action without resolvable monetary authority fails closed.** No resolver, no Authority Graph proof for the decision, a lineage that differs from the proof, a revoked/suspended/expired hop, no `max_amount`, ceilings only in another asset, no `spending_limit` in the asset, any malformed constraint, an unrecognized asset or scale overflow, or another organization → `financial-authority-withheld` (public `withheld` / `authority-binding`) with a `FINANCIAL_AUTHORITY_*` code; no grant, reservation or adapter call; the Kernel decision is reported unchanged. Missing authority is never defaulted, never unlimited, never the request's amount. | PATH-LOCAL to the governed-action / bounded-grant lifecycle | RUNTIME + TEST-ENFORCED | fail-closed table and tenant row in `authority-payment-ceilings.test.ts`; resolver rows in `kernel-authority-monetary-constraints.test.ts` | Financial authority is resolvable only for actors whose recognition consults the Authority Graph (agents, and actors acting for a principal). |
| SEC-INV-097 (**PAY-04**) | **The ceiling belongs to the lineage that authorized the action, and delegation cannot escape it.** At issuance the resolved chain must equal the decision's own `AuthorityProof` (`evaluatedGrantIds` / `evaluatedDelegationIds`, in order); every monetary constraint on every hop applies; the effective ceiling is the narrowest; a delegate may add restrictions, never drop, raise or re-denominate an upstream one. Assets are never compared across. | PATH-LOCAL to the governed-action / bounded-grant lifecycle | RUNTIME + TEST-ENFORCED | delegation rows (repeat-nothing, narrow, broaden, aggregate) in `authority-payment-ceilings.test.ts`; forged/absent decision-id, delegation and asset rows in `kernel-authority-monetary-constraints.test.ts` | Rotating authority for the same delegate is shadowed by the Authority Graph's first-match selection (withheld, never widened). |
| SEC-INV-098 (**PAY-05**) | **Durable spending limits are mandatory, stable and cannot be removed by the caller or by host P7 policy.** For a financial exercise every `spending_limit` on the lineage is appended to the host policy's answer (never consulted about it) under a namespaced, authority-anchored bucket (`authority:<limitId>`, scope = organization + authority record + asset) — never keyed by request, decision, grant, execution or process — so new grants share the bucket and distinct authorities or assets never do; a colliding host bucket refuses the whole policy (`EXERCISE_CONTROL_POLICY_INVALID`). | PATH-LOCAL to the governed-action / bounded-grant lifecycle | RUNTIME + TEST-ENFORCED | §88 bucket rows, host-narrowing, empty-host-policy, collision, authority- and asset-isolation rows in `authority-payment-ceilings.test.ts` | — |
| SEC-INV-099 (**PAY-06**) | **Monetary authority is exact and durable across restart; consumption is durable too.** Constraints are canonical decimal text validated on append (both stores, any caller), at provisioning (trusted registry) and on hydration (malformed → the world fails closed); they live in the event payload and so in the event digest (widening or deleting one in the file fails integrity); no number conversion exists between store, projection, resolver, `ExerciseControlLimit.maximum` and the SQLite reservation. Definitions (Kernel Authority Store) and consumption (P7 ledger) are separate state and both survive restart; no budget resets. | PATH-LOCAL to the governed-action / bounded-grant lifecycle | STRUCTURAL + RUNTIME + TEST-ENFORCED | round-trip, integrity, validation and hydration rows in `kernel-authority-monetary-constraints.test.ts`; restart (175 → reopen → 75 / 0.01), > 2^53 and 0.1 + 0.2 rows in `authority-payment-ceilings.test.ts`; number-conversion scans in `authority-payment-ceilings-structure.test.ts` | Unkeyed digests: integrity, not authenticity (SEC-TRUST-002). |
| SEC-INV-100 (**PAY-07**) | **Authority changes invalidate stale financial authority before any effect.** The financial authority is re-resolved inside the synchronous grant-commit guard and must equal the measured one exactly (a revocation or narrowing in the window commits nothing); a financial grant's `authorityBindingDigest` commits to it (`grantAuthorityProvenanceDigest`), and P7 revalidation recomputes it from the current authority before the policy and the reservation and again after — revoked, expired, narrowed or replaced authority withholds with no reservation and no adapter call; a pre-P10 financial grant (binding-only provenance) can never match. Non-financial provenance is byte-identical to pre-P10. | PATH-LOCAL to the governed-action / bounded-grant lifecycle | STRUCTURAL + RUNTIME + TEST-ENFORCED | commit-window revoke/narrow, post-issuance revoke and replacement rows in `authority-payment-ceilings.test.ts`; legacy-grant and digest rows plus commit-guard scan in `authority-payment-ceilings-structure.test.ts` | Not atomic with another process's provisioning: that process's writes are observed on reload/restart (`AOC_DURABLE_KERNEL_AUTHORITY.md`). |
| SEC-INV-101 (**PAY-08**) | **P7 admission stays atomic across every applicable financial limit.** Authority and host limits enter one validated snapshot and one `reserve()`; a refusal on any limit writes nothing for any; concurrent attempts on the last capacity admit at most one, in one Host or across Hosts sharing the durable files; P7's settle/release rules are unchanged (executed and unconfirmed consume, confirmed failure releases; no TTL, sweeper or reset). | PATH-LOCAL to the governed-action / bounded-grant lifecycle | RUNTIME + TEST-ENFORCED | partial-admission, concurrency (one Host, two Hosts), failed-release, unconfirmed-consumed and rolling-window rows in `authority-payment-ceilings.test.ts`; P7's own worker-thread races | — |

### 4.11 Durable monetary outcomes and provider certainty (P11)

Alias ids `OUTCOME-0x` are given so these can be cited from `docs/architecture/ADR-DURABLE-MONETARY-OUTCOMES.md`. Integrity here is unkeyed SHA-256 tamper detection, not authenticity (P20), and no invariant below claims exactly-once execution or complete outcome capture across process death: no local commit is atomic with an external effect.

| ID | Invariant | Scope | Enforcement | Evidence | Boundary |
|---|---|---|---|---|---|
| SEC-INV-102 (**OUTCOME-01**) | **The exact execution context is durable before the provider crossing.** No adapter is invoked for a governed action unless its attempt (tenant; evaluation, request, decision, grant and execution ids by reference; action; the exact P9 amount and asset the adapter receives as `ValidatedExecutionAction.amount`) was first durably prepared in the execution outcome store, before the write-ahead claim. A preparation that cannot be proven written stops the request before the claim (`system_error` / `GOVERNED_ACTION_EXECUTION_CLAIM_FAILED`) — nothing stranded; preparation is idempotent and says "prepared", never "attempted". | PATH-LOCAL to the governed-action / bounded-grant lifecycle | STRUCTURAL + RUNTIME + TEST-ENFORCED | order scans in `execution-outcome-boundaries.test.ts`; preparation, crash-before-claim and prepare-failure rows in `durable-monetary-outcomes.test.ts`; §140 row in `durable-monetary-outcomes-e2e.test.ts` | A crash after the claim leaves the attempt unresolved (P12). |
| SEC-INV-103 (**OUTCOME-02**) | **One provider-neutral certainty, derived once.** `completed` → `confirmed-completed`, `failed` → `confirmed-not-completed`, `unconfirmed` → `unconfirmed`; a withholding has no certainty. The store records the certainty and never a status beside it; impossible combinations (a completion with a failure, an unconfirmed effect with a reason, a withholding with an adapter or reference) are refused on write and on read. Certainty is neither authorization nor settlement. | PATH-LOCAL to the governed-action / bounded-grant lifecycle | STRUCTURAL + RUNTIME + TEST-ENFORCED | closed-contract rows in `execution-outcome-store.test.ts`; `provider-certainty.ts` exhaustive mappings | — |
| SEC-INV-104 (**OUTCOME-03**) | **Initial observations are immutable.** One attempt and one initial observation per execution id (primary key in both SQLite tables; triggers refuse `UPDATE`/`DELETE`; every write decision one `BEGIN IMMEDIATE` transaction). The identical fact is idempotent; any other — another certainty, reference, attribution, reason or instant — is `EXECUTION_OUTCOME_CONFLICT`, never last-write-wins, never chosen by time; a later resolution is a different artifact (P12). | COMPONENT-LOCAL to the execution outcome store | RUNTIME + TEST-ENFORCED | conflict rows (both stores), trigger rows, and the cross-thread races in `execution-outcome-concurrency.test.ts` | — |
| SEC-INV-105 (**OUTCOME-04**) | **Unconfirmed is never rewritten as failed, and a persistence failure never rewrites provider truth.** An unconfirmed observation conflicts with any later completion or failure; a non-string `providerRef` on `failed`/`unconfirmed` is dropped rather than invalidating the result into `ADAPTER_ERROR`; a failed observation write leaves the live result, P7's settle/release and the retry ban exactly as they were (`outcomeRecorded: false`), and is never retried. | PATH-LOCAL to the governed-action / bounded-grant lifecycle | RUNTIME + TEST-ENFORCED | unconfirmed rows in `execution-outcome-store.test.ts` and `execution-adapter-registry.test.ts`; lost-observation rows in `durable-monetary-outcomes*.test.ts` | — |
| SEC-INV-106 (**OUTCOME-05**) | **`providerRef` never changes certainty and never carries a secret or destination.** It may accompany any provider certainty; it is carried only when `isRecordableProviderRef` accepts it (1–512 printable ASCII, trimmed, no bearer/basic credential, JWT, PEM, URL, cookie or authorization shape) — applied by the execution runtime for every adapter and again by the store — and Generic HTTP adds its configured-credential echo filter and never fabricates one where no response exists. No adapter `detail`, body, header, URL or credential is persisted. | PATH-LOCAL to the governed-action / bounded-grant lifecycle | STRUCTURAL + RUNTIME + TEST-ENFORCED | reference rows in `execution-adapter-registry.test.ts`, `generic-http-execution-adapter.test.ts`, `execution-outcome-store.test.ts` | Literal non-disclosure only; a provider that encodes a secret is not detectable. |
| SEC-INV-107 (**OUTCOME-06**) | **P8 cannot become the replay source, and evidence cannot outrank the canonical record.** Replay of a claimed execution reads the execution outcome store — never the event stream, process memory or the provider — and the Governance outcome summary is written only after the observation committed, pointing at it by digest; a forged summary row cannot rewrite a P11 observation; the legacy summary is read only for executions with no P11 record. | PATH-LOCAL to the governed-action / bounded-grant lifecycle | STRUCTURAL + RUNTIME + TEST-ENFORCED | single-read and holder scans in `execution-outcome-boundaries.test.ts`; P8-failure and forged-summary rows in `durable-monetary-outcomes*.test.ts` | — |
| SEC-INV-108 (**OUTCOME-07**) | **Replay never invokes a provider.** An execution identity the write-ahead claim records is answered from durable state; claim-only (no observation) is `…_ALREADY_ATTEMPTED` and distinct from a recorded `unconfirmed` (`…_OUTCOME_UNCONFIRMED`); nothing retries, polls or reconciles. | PATH-LOCAL to the governed-action / bounded-grant lifecycle | STRUCTURAL + RUNTIME + TEST-ENFORCED | adapter-count-zero restart rows in `durable-monetary-outcomes-e2e.test.ts`; no-timer/no-retry scans in `execution-outcome-boundaries.test.ts` | Claim-only and unconfirmed executions stay unresolved until P12. |
| SEC-INV-109 (**OUTCOME-08**) | **A corrupted or unreadable outcome never replays as success.** Every read re-validates the closed contract (canonical money, closed certainty, recordable attribution and reference) and recomputes the attempt and observation digests (the observation's commits to its attempt's); a failure is `EXECUTION_OUTCOME_CORRUPT`, never repaired, and the replay posture is `…_ALREADY_ATTEMPTED` with no fallback and no adapter call. | PATH-LOCAL to the governed-action / bounded-grant lifecycle | RUNTIME + TEST-ENFORCED | raw-SQLite corruption rows in `execution-outcome-store.test.ts`; on-disk tamper row in `durable-monetary-outcomes-e2e.test.ts`; unreadable-store row in `durable-monetary-outcomes.test.ts` | Unkeyed digests: a consistent rewrite of a row and its digest is not detected (P20). |
| SEC-INV-110 (**OUTCOME-09**) | **Outcome persistence cannot alter authorization history or grant authority, and a caller cannot self-report it.** The store's port records and reads only; no Kernel, grant, exercise-control, emergency-control, customer-identity, authority or adapter module imports it; a past success is never permission; the intent contract has no provider-result field and the P11 self-report vocabulary (`providerRef`, `providerStatus`, `providerCertainty`, `certainty`, `executionOutcome`, `executionStatus`, `outcomeRecorded`) is a reserved `assertedContext` key; only the runtime's normalized, registry-authenticated outcome is recorded, tenant-confined with no `system` escape. | PATH-LOCAL to the governed-action / bounded-grant lifecycle | STRUCTURAL + RUNTIME + TEST-ENFORCED | `execution-outcome-boundaries.test.ts`; self-report and forged-attribution rows in `durable-monetary-outcomes.test.ts`; tenant rows in `execution-outcome-store.test.ts` | — |

### 4.12 Execution reconciliation and resolution authority (P12)

Alias ids `RESOLVE-0x` are given so these can be cited from `docs/architecture/ADR-EXECUTION-RECONCILIATION-AND-RESOLUTION-AUTHORITY.md`. P12 is **optional** composition (`executionReconciliation`); every invariant below holds when it is enabled, and omitting it leaves P11 exactly as it was. Integrity is unkeyed SHA-256, not authenticity (P20). No invariant claims exactly-once execution: P12 closes uncertainty only when a trusted authority can later prove what happened, and an execution may stay unresolved forever.

| ID | Invariant | Scope | Enforcement | Evidence | Boundary |
|---|---|---|---|---|---|
| SEC-INV-111 (**RESOLVE-01**) | **A resolution comes only from the execution's durably bound authority.** A resolution row requires the binding on record by execution, attempt digest, binding digest and authority (`EXECUTION_RESOLUTION_NOT_BOUND` otherwise); reconciliation asks only the bound authority, and a bound authority that is no longer composed is `authority-unavailable / not-composed` — never substituted, never re-selected, never reconstructed from current routing or configuration. | PATH-LOCAL to the governed-action / bounded-grant lifecycle | RUNTIME + TEST-ENFORCED | not-bound rows in `execution-resolution-store.test.ts`; restart-with-changed-selector and removed-authority rows in `execution-reconciliation-e2e.test.ts` | — |
| SEC-INV-112 (**RESOLVE-02**) | **With P12 enabled, the authority is bound before the claim.** Every new governed execution is durably bound after P11 preparation and before the write-ahead claim; a selector that throws, answers a promise, an object, a blank or an unknown id, or a store that cannot bind, stops the request before the claim and the provider (`system_error` / `GOVERNED_ACTION_EXECUTION_CLAIM_FAILED`, provider calls 0, retry safe). A retry reuses its binding; there is no fallback authority. | PATH-LOCAL to the governed-action / bounded-grant lifecycle | STRUCTURAL + RUNTIME + TEST-ENFORCED | order scan in `execution-reconciliation-boundaries.test.ts`; selector and bind-failure rows in `execution-reconciliation-e2e.test.ts` | Legacy executions are bound only by explicit trusted adoption. |
| SEC-INV-113 (**RESOLVE-03**) | **A caller cannot self-resolve.** No route, SDK method, intent field or governed-action path reaches reconciliation; the P12 vocabulary (`resolution`, `resolved`, `reconciled`, `reconciliation`, `resolutionAuthority`, `resolutionAuthorityId`, `providerResolution`, `providerOutcome`, `finalOutcome`, `confirmedCompleted`, `confirmedNotCompleted`) joins the reserved `assertedContext` keys; the authority is told the P11 `providerRef`, never a caller's. | PATH-LOCAL to the governed-action / bounded-grant lifecycle | STRUCTURAL + RUNTIME + TEST-ENFORCED | self-report rows in `execution-reconciliation-e2e.test.ts`; no-route scan in `execution-reconciliation-boundaries.test.ts` | — |
| SEC-INV-114 (**RESOLVE-04**) | **Ordinary replay never queries a provider or an authority.** The orchestrator holds a binder and a read-only resolution reader — no authority, no `resolve`, no reconciliation service; replay reads durable state only, and only the trusted in-process `executionReconciliation.reconcile` asks the bound authority, at most once per call, with no timer, poll or background job. | PATH-LOCAL to the governed-action / bounded-grant lifecycle | STRUCTURAL + RUNTIME + TEST-ENFORCED | orchestrator holder scans in `execution-reconciliation-boundaries.test.ts`; resolver-count rows in `execution-reconciliation-e2e.test.ts` | — |
| SEC-INV-115 (**RESOLVE-05**) | **P11 history is never rewritten.** A resolution is a separate row in a separate store that commits to the P11 attempt digest and, when it resolves one, the P11 observation digest; the P11 store has no P12 table, column or writer, and its observation (and P8's `execution.outcome.observed`, and the Governance P11 summary) stays exactly as recorded. | COMPONENT-LOCAL to the execution resolution store | STRUCTURAL + RUNTIME + TEST-ENFORCED | P11-untouched scan in `execution-reconciliation-boundaries.test.ts`; P11-unchanged rows in `execution-reconciliation-e2e.test.ts` | — |
| SEC-INV-116 (**RESOLVE-06**) | **A resolution is definitive, closed and immutable.** `confirmed-completed`, or `confirmed-not-completed` with an existing `ExecutionFailureReason`; the authority's answer is read from data descriptors only and refused whole on any undeclared key (amount, asset, grant, budget, confidence), unknown certainty or invalid reason; one resolution per execution (primary key, `BEGIN IMMEDIATE`, triggers refuse `UPDATE`/`DELETE`); the identical answer is idempotent, a different one `conflict` — never last-write-wins; `unresolved` writes nothing. | COMPONENT-LOCAL to the execution resolution store | RUNTIME + TEST-ENFORCED | closed-contract, conflict and trigger rows in `execution-resolution-store.test.ts`; adversarial-answer and racing-host rows in `execution-reconciliation-e2e.test.ts` | — |
| SEC-INV-117 (**RESOLVE-07**) | **Capacity returns only for a canonical non-completion.** The only permitting-direction P7 mutation is one immutable resolution row, bound to the P12 resolution digest, written only through the narrow `ExerciseControlReconciliationPort` the reconciliation service alone holds; the gate cannot record one and P12 cannot reserve, settle or release; the original terminal event is never rewritten; `confirmed-completed` never creates capacity; a not-completed row stops the reservation consuming in every bucket at once, and not at all if the runtime's own history contradicts it. | PATH-LOCAL to the governed-action / bounded-grant lifecycle | STRUCTURAL + RUNTIME + TEST-ENFORCED | `exercise-reservation-resolution-contract.ts` (both ledgers); narrow-capability scans in `execution-reconciliation-boundaries.test.ts`; flagship and multi-limit rows in `execution-reconciliation-e2e.test.ts` | — |
| SEC-INV-118 (**RESOLVE-08**) | **The resolution commits before any capacity returns.** Resolution → P7 row → evidence; a resolution that cannot be written moves no capacity; a crash or P7 outage after the resolution leaves capacity conservatively consumed, and the next explicit reconcile applies the row idempotently without asking the authority again. | PATH-LOCAL to the governed-action / bounded-grant lifecycle | STRUCTURAL + RUNTIME + TEST-ENFORCED | order scan in `execution-reconciliation-boundaries.test.ts`; append-failure and crash-gap rows in `execution-reconciliation-e2e.test.ts` | No cross-store transaction exists; the gap fails toward availability loss. |
| SEC-INV-119 (**RESOLVE-09**) | **Corrupt state never increases capacity or replays as an answer.** A corrupt P11 basis is never reconciled (the authority is not asked); a corrupt binding or resolution is `basis-unavailable`, never replayed and never overwritten; a P7 resolution row is verified before it can stop anything consuming, so a tampered row fails its bucket closed and a deleted one only makes capacity consumed again. | PATH-LOCAL to the governed-action / bounded-grant lifecycle | RUNTIME + TEST-ENFORCED | tamper rows in `execution-resolution-store.test.ts`, `exercise-control-resolution-sqlite.test.ts` and `execution-reconciliation-e2e.test.ts` | Unkeyed digests: a consistent rewrite and re-seal is not detected (P20). |
| SEC-INV-120 (**RESOLVE-10**) | **Unresolved never causes a retry or a release, and time is never evidence.** An `unresolved`, failed or invalid answer writes nothing, moves no capacity and invokes no adapter; nothing retries, resubmits or re-executes the original effect, and no TTL, timeout or sweeper converts uncertainty into an answer. A proven non-completion returns capacity only: the claim stays and the execution id is never re-run. | PATH-LOCAL to the governed-action / bounded-grant lifecycle | STRUCTURAL + RUNTIME + TEST-ENFORCED | no-execute / no-timer scans in `execution-reconciliation-boundaries.test.ts`; unresolved and adapter-count rows in `execution-reconciliation-e2e.test.ts` | — |
| SEC-INV-121 (**RESOLVE-11**) | **Returned capacity and new admissions serialize.** The P7 resolution row is decided inside the ledger's own `BEGIN IMMEDIATE`, in the same database as admission; racing a returned 100 against two new 100s in parallel threads admits at most one, and a completed resolution racing new reservations admits none. | COMPONENT-LOCAL to the exercise-control ledger | RUNTIME + TEST-ENFORCED | worker-thread races in `exercise-control-resolution-sqlite.test.ts` | One SQLite file on one host; not distributed. |
| SEC-INV-122 (**RESOLVE-12**) | **A resolution authority has no spending authority and no generic reach.** P12 modules import no Kernel, grant, policy, bounded-grant, emergency-control or adapter module; the authority's answer has no field for an amount, asset, grant, budget or decision; P12 core never parses, dereferences or fetches a `providerRef`, adds no status-URL configuration and contains no Stripe, MPP or XRPL code, settlement or receipt semantics. | PATH-LOCAL to the governed-action / bounded-grant lifecycle | STRUCTURAL + TEST-ENFORCED | `execution-reconciliation-boundaries.test.ts` | A host-written authority is trusted code; its compromise can falsely release or retain capacity (SEC-TRUST-009). |

### 4.13 MPP challenge adaptation and business-level idempotency (P13)

Alias ids `MPP-0x` are given so these can be cited from `docs/architecture/ADR-MPP-CHALLENGE-AND-BUSINESS-IDEMPOTENCY.md`. P13 is **optional** composition (`mppChallengePayments`) and client-side only: Frontera receives a merchant's MPP `Payment` challenge; it never issues one. P13 is an ingress transformation onto the existing governed-action path — every invariant of §4.8 … §4.12 still decides the effect — and adds no effect path (NO_BYPASS §5.11). No invariant here claims a challenge is authentic: a client cannot verify the server's challenge binding. Integrity is unkeyed SHA-256, not authenticity (P20).

| ID | Invariant | Scope | Enforcement | Evidence | Boundary |
|---|---|---|---|---|---|
| SEC-INV-123 (**MPP-01**) | **A business operation never derives from a challenge.** The governed idempotency key is `aoc.mpp.bop:` + SHA-256 over a domain-tagged JSON array of organization, principal and the caller-named `businessOperationId`; neither it nor the request id reads a challenge `id`, `expires`, `opaque`, realm, method, clock or randomness, and the request id is computed by the orchestrator's own `deriveGovernedActionRequestId`, imported. | PATH-LOCAL to MPP challenge payments | STRUCTURAL + RUNTIME + TEST-ENFORCED | derivation scans in `mpp-challenge-boundaries.test.ts`; refresh rows in `mpp-challenge-service.test.ts`; §87 agreement row in `mpp-challenge-payments-e2e.test.ts` | A caller that reuses one `businessOperationId` for two purchases gets one; one that invents a new id per retry gets many. The name is the caller's responsibility. |
| SEC-INV-124 (**MPP-02**) | **One business operation, one meaning.** At most one operation per (organization, principal, businessOperationId), enforced by a SQLite primary key under `BEGIN IMMEDIATE`; the same semantic digest is idempotent, a different amount, asset, counterparty, resource, action, HTTP method, body digest or external order id is `business-operation-conflict` — never an overwrite, never last-write-wins — and is refused before `govern()`. | COMPONENT-LOCAL to the MPP business-operation store | RUNTIME + TEST-ENFORCED | mutation rows in `mpp-business-operation-store.test.ts` and `mpp-challenge-service.test.ts`; 10-vs-11 worker race in `mpp-business-operation-concurrency.test.ts`; §240 row in `mpp-challenge-payments-e2e.test.ts` | One SQLite file on one host; the process-local store gives no idempotency across a restart. |
| SEC-INV-125 (**MPP-03**) | **The business operation and its challenge are durable before governance.** `store.record` (operation + challenge, one transaction) completes before `orchestrator.govern()` is called; a store that throws, a non-canonical store clock, a conflict or a full history returns a refusal with no Kernel call, grant, P7 reservation, claim or adapter call. | PATH-LOCAL to MPP challenge payments | STRUCTURAL + RUNTIME + TEST-ENFORCED | order scan in `mpp-challenge-boundaries.test.ts`; store-failure rows in `mpp-challenge-service.test.ts` | — |
| SEC-INV-126 (**MPP-04**) | **A challenge proposes; it never authorizes.** The adapted action is an ordinary `GovernedActionIntent` (action, resource, counterparty, amount, derived key — no MPP field, no `assertedContext`) validated by the orchestrator's own validator; the Kernel, P10 ceilings and P7 limits decide exactly as for any governed action, and a merchant asking more than authority is withheld with no provider effect. No normalizer, resolver or challenge field can state authorization, a ceiling, a budget or a grant. | PATH-LOCAL to MPP challenge payments | STRUCTURAL + RUNTIME + TEST-ENFORCED | intent-shape and authority-field rows in `mpp-challenge-service.test.ts`; P10 / P7 rows in `mpp-challenge-payments-e2e.test.ts`; no-Kernel scans in `mpp-challenge-boundaries.test.ts` | — |
| SEC-INV-127 (**MPP-05**) | **A challenge cannot report an outcome.** Nothing a challenge or normalizer carries (`paid`, `settled`, `receipt`, `providerRef`, …) reaches P11, P12 or a result; the store has no status or outcome column; what an execution did is answered only by the existing P11 / P12 replay. | PATH-LOCAL to MPP challenge payments | STRUCTURAL + RUNTIME + TEST-ENFORCED | outcome-shaped-field rows in `mpp-challenge-service.test.ts`; column scan in `mpp-business-operation-store.test.ts` | — |
| SEC-INV-128 (**MPP-06**) | **MPP money stays exact, and no FX exists.** A trusted method normalizer maps base units and a method currency to P9 decimal text in a registry asset; its answer is re-read from data descriptors and parsed by `parseMonetaryAmount` (canonical, recognized, within scale, positive); money is `TEXT` in SQLite; no P13 code calls `Number()`, `parseFloat`, `parseInt`, `toFixed` or `Math` rounding, and no asset is ever converted into another. | PATH-LOCAL to MPP challenge payments | STRUCTURAL + RUNTIME + TEST-ENFORCED | `9007199254740993.01` rows in all three P13 suites (through restart in the E2E); number scans in `mpp-challenge-boundaries.test.ts` | The normalizer's currency and decimals mapping is trusted host code (SEC-TRUST-010). |
| SEC-INV-129 (**MPP-07**) | **A body-bearing paid request is bound to its body.** For `POST`, `PUT`, `PATCH` or any digested request, a challenge is usable only when its RFC 9530 `digest` shares a recognized algorithm with the trusted `contentDigest` and agrees on every shared one; a body-bearing request without a trusted digest, a challenge without a digest, a mismatch, or a digest on a bodiless request is refused. The canonical body digest is part of the business semantics. | PATH-LOCAL to MPP challenge payments | RUNTIME + TEST-ENFORCED | body-binding rows in `mpp-challenge-service.test.ts`; digest rows in `mpp-challenge-protocol.test.ts` | The network layer computes `contentDigest` over the bytes it sends (trusted). |
| SEC-INV-130 (**MPP-08**) | **An expired challenge is unusable, and expiry never erases a business operation.** `expires` is judged strictly (RFC 3339) against the injected clock at ingestion; a refreshed challenge after expiry is another instance of the same operation and the same request. The P14 reader returns `expires` for re-checking at credential time; ingestion-time validity is not claimed to last. | PATH-LOCAL to MPP challenge payments | RUNTIME + TEST-ENFORCED | expiry rows in `mpp-challenge-protocol.test.ts` and `mpp-challenge-service.test.ts` | — |
| SEC-INV-131 (**MPP-09**) | **A refreshed challenge never creates a new execution identity, and never bypasses P11 / P12.** A refresh appends an immutable challenge instance and presents the byte-identical governed intent, which the Governance Store replays before the Kernel; executed, failed, unconfirmed and P12-resolved outcomes replay unchanged, adapter calls stay at one, and a P12 `confirmed-not-completed` with returned capacity still replays `execution_failed` — the claimed execution is never retried; a new purchase needs a new `businessOperationId`. | PATH-LOCAL to MPP challenge payments | RUNTIME + TEST-ENFORCED | §142 … §146 rows in `mpp-challenge-payments-e2e.test.ts`; 100-concurrent-submission rows there | — |
| SEC-INV-132 (**MPP-10**) | **The raw challenge never crosses `ValidatedExecutionAction` or the Kernel.** The adapter boundary declares exactly its pre-P13 fields; a future payment adapter reads the verified challenge through the read-only `MppChallengeContextReader`, keyed by the trusted `correlation.requestId` and tenant — never by a challenge id a caller or adapter supplies; the Kernel, P10, P7, P11, P12, P8 and the orchestrator import no P13 module. | PATH-LOCAL to MPP challenge payments | STRUCTURAL + RUNTIME + TEST-ENFORCED | field pin and import scans in `mpp-challenge-boundaries.test.ts`; handoff row in `mpp-challenge-payments-e2e.test.ts` | — |
| SEC-INV-133 (**MPP-11**) | **P13 holds no credential and no secret.** No P13 code constructs a Payment credential, reads a customer `Authorization` value, or contains Stripe, EVM, XRPL, wallet, key, seed or signature code; the store has no credential, authorization, secret, token or signature column, and its closed contract refuses any undeclared field. | PATH-LOCAL to MPP challenge payments | STRUCTURAL + RUNTIME + TEST-ENFORCED | P14 scans in `mpp-challenge-boundaries.test.ts`; column and closed-contract rows in `mpp-business-operation-store.test.ts` | — |
| SEC-INV-134 (**MPP-12**) | **Method normalizers, the counterparty resolver and the selector are trusted, snapshotted and fenced — and none can authorize.** Composed once (unique `methodId/charge`, synchronous functions); their answers are read as untrusted executable output — a getter, Proxy, promise, class instance, symbol key, number amount or any undeclared key refuses the challenge; a normalizer cannot name the business operation, the governed key or the counterparty; a merchant string becomes a counterparty only through the trusted resolver; the selector is asked even for one candidate and must name exactly one — no first-wins. | PATH-LOCAL to MPP challenge payments | RUNTIME + TEST-ENFORCED | normalizer, resolver, selector and composition rows in `mpp-challenge-service.test.ts` | Their truthfulness is trusted host code (SEC-TRUST-010). |
| SEC-INV-135 (**MPP-13**) | **The challenge parser is standards-correct, bounded and fail-closed.** RFC 9110 §11 tokenization (quoted strings, multiple challenges and field values, token68) — never a comma split; required parameters exactly once; duplicates refused; unknown parameters dropped unread; strict base64url, fatal UTF-8, JCS round trip, bounded depth and members, no prototype-shaped names; every size bounded before allocation. | COMPONENT-LOCAL to `mpp-challenge/protocol.ts` | RUNTIME + TEST-ENFORCED | `mpp-challenge-protocol.test.ts` | The draft publishes no client vectors; coverage is built from its normative text and its HMAC-vector encodings. |
| SEC-INV-136 (**MPP-14**) | **Corrupt business state is never "no previous operation".** Every read verifies the operation and every challenge row — record digests, recomputed semantic digest, derived key and request id, challenge digests, contiguous sequences, at least one challenge — before any row's sequence can select it; tampering is `MPP_BUSINESS_OPERATION_CORRUPT` for reads and writes alike, and a service refusal before governance. Triggers refuse `UPDATE` and `DELETE`; an unknown schema version is refused before the file is touched. | COMPONENT-LOCAL to the MPP business-operation store | RUNTIME + TEST-ENFORCED | tamper, trigger and schema rows in `mpp-business-operation-store.test.ts` | Unkeyed digests: a consistent rewrite and re-seal is not detected (P20). The deterministic request id means even an undetected deletion replays the same governed request. |
| SEC-INV-137 (**MPP-15**) | **A caller cannot inject trusted P13 state.** `POST /api/governed-actions` refuses `businessOperationId`, `mppChallenge`, `paymentChallenge`, `challengeId`, `mppMethod`, `mppIntent` and `mppRealm` as undeclared, and `businessOperationId`, `challenge`, `challengeId`, `mppChallenge`, `mppMethod`, `mppIntent`, `mppRealm`, `paymentChallenge`, `paymentCredential` and `merchantRealm` join the reserved `assertedContext` keys; P13 is reachable only through the trusted in-process `AocEnterprise.mppChallengePayments` — no route, no SDK method. | PATH-LOCAL to the governed-action / bounded-grant lifecycle | STRUCTURAL + RUNTIME + TEST-ENFORCED | self-injection rows in `mpp-challenge-payments-e2e.test.ts`; no-route scan in `mpp-challenge-boundaries.test.ts` | — |

---

## 5. Execution Path Guarantees

Frontera has **four** distinct effect models. Conflating them is the single largest overclaim risk in this repository.

> **Complete inventory.** These four models are the *shapes*. The exhaustive, per-path enumeration — fifty-five production-capable effect paths, each with a stable `EP-` id and exactly one classification — is `docs/security/NO_BYPASS_AUTHORITY_CONTROLLED_EXECUTION.md` §5. Rule 4 of §9 below is satisfied by adding a path there as well as here.

### 5.1 Evaluation — `AocKernel.evaluate()` / `POST /api/governance/evaluate`

- **Guarantees:** a decision, with reason codes, a trace, and a durably committed Governance Record. Recognition precedes any allow. Narrowing-only composition. No mutation of the request.
- **Does not guarantee, and does not attempt:** anything about execution. It invokes nothing.
- **Invariants:** SEC-INV-001 … SEC-INV-008.

### 5.2 Decision-time enforcement — `AocKernel.enforce()`

- **Guarantees:** the declared action passed authorization — recognition, authority, policy, context and blocking obligations — *before* the supplied executor was invoked, and the executor is not invoked at all on any non-allow outcome.
- **Does not guarantee:** that the executor's actual external effect corresponds to the declared `ActionDescriptor`. The executor is an opaque closure and is never inspected.
- **The correct formulation:**

  > `AocKernel.enforce()` = **decision-time** authorization of a **declared action**.
  > Bounded-grant exercise = **effect-time** authorization of a **validated execution action**.

- This is an accepted architectural boundary, not a defect. No accepted ADR gives grants a role in the executor gate, and grants deliberately do not gate `enforce()`.
- **Invariants:** SEC-INV-009 (guarantee), SEC-INV-010 (limit).

### 5.3 Bounded-grant exercise — `GrantExecutionService` / `AuthorityControlledExecutionService`

- **Guarantees:** the strongest execution-time property in the repository. The authoritative store is re-read on every attempt; twelve checks run fail-closed; every failing axis is reported; the adapter receives only values an assessment proved, across a boundary with no free-form payload channel and a build-enforced 17-field whitelist.
- **Does not guarantee:** anything about the other three paths. It is **opt-in**. Since P5 it has exactly one HTTP entry, `POST /api/governed-actions`, which enters at the **top** — customer admission, then the Governed Action Orchestrator — and exposes no grant, exercise, routing or emergency-control surface (SEC-INV-055 … SEC-INV-061).
- **Invariants:** SEC-INV-011 … SEC-INV-018, SEC-INV-027, and — when a deployment composes them — SEC-INV-039 … SEC-INV-054, for the customer route SEC-INV-055 … SEC-INV-061, for P7 exercise controls SEC-INV-070 … SEC-INV-079, for the P8 canonical authority event stream SEC-INV-080 … SEC-INV-088, for P9 canonical monetary semantics SEC-INV-089 … SEC-INV-093, for P10 authority-sourced payment ceilings and durable spending limits SEC-INV-094 … SEC-INV-101, for P11 durable monetary outcomes and provider certainty SEC-INV-102 … SEC-INV-110, and — when composed — for P12 execution reconciliation and resolution authority SEC-INV-111 … SEC-INV-122, and for P13 MPP challenge adaptation and business-level idempotency SEC-INV-123 … SEC-INV-137.
- **Since P12:** when `executionReconciliation` is composed, every new governed execution is durably bound to one trusted resolution authority before its claim; an execution P11 left uncertain can later be resolved — only by that authority, only through the trusted in-process reconciliation service, never by replay, a caller or a retry — into a new immutable resolution that never rewrites P11; a proven non-completion returns its P7 capacity through a second immutable ledger row bound to the resolution digest, committed only after the resolution (SEC-INV-111 … SEC-INV-122).
- **Since P13:** when `mppChallengePayments` is composed, a merchant's MPP `Payment` challenge can enter this path — and only this path — as an ordinary governed intent: a caller-named business operation, durably recorded with its immutable challenge history **before** governance, maps deterministically to one governed request, so a refreshed challenge, a retry or a restart replays what that request already did and never mints a second execution identity. No effect path, route or authority source is added (SEC-INV-123 … SEC-INV-137).
- **Since P11:** every governed execution's exact prepared context is durable before its claim and provider crossing, and its initial provider observation — `confirmed-completed`, `confirmed-not-completed` or `unconfirmed`, with attribution and an opaque `providerRef` — is durable and immutable once obtained; replay answers from it without a provider call, and an unobserved or unverifiable execution stays unresolved and is never retried (SEC-INV-102 … SEC-INV-110).
- **Since P10:** a host-classified financial action is issued a grant only under durable monetary authority on the lineage that authorized it — the grant's amount ceiling is that authority's `max_amount`, never the requested amount — and its exercise consumes the authority's durable `spending_limit`s through P7's one atomic reservation, with the financial authority revalidated at the commit boundary and before and after the reservation (SEC-INV-094 … SEC-INV-101).
- **Since P8:** when governed actions are composed, the lifecycle on this path is also recorded as a canonical, ordered, hash-chained, tenant-confined event stream — after each fact, never before it, read by nothing that decides and **never awaited by authority control flow** (SEC-INV-080 … SEC-INV-088). Evidence only: it adds no authorization guarantee and cannot weaken any check above. Projection still shares the process and event loop and may add process latency; see SEC-INV-088.
- **Since P7:** when `exerciseControls` is composed, repeated use is bounded too — aggregate count, aggregate amount and rolling velocity limits admitted atomically through a durable reservation before the adapter, conservative settlement, and exact-equality revalidation of the grant's authority binding before and after the reservation, with the grant itself re-read and re-assessed after the reservation (SEC-INV-070 … SEC-INV-079). Path-local: per-action containment elsewhere is unchanged, and no other path gains aggregate control.
- **Since Prompt 4:** the adapter this path invokes may be the composite `createExecutionAdapterRegistry(...)`, which routes server-side to exactly one trusted child (SEC-INV-039 … SEC-INV-042); and, when composed, a durable operational interlock is consulted at four points and fails closed when unreadable (SEC-INV-043 … SEC-INV-052). Neither changes what authorizes an action: routing chooses where, and the interlock can only withhold.

### 5.3a Generic HTTP adapter outbound request (P6) — EP-050

- **Guarantees:** when the bounded-grant path routes to a Generic HTTP child, at most one HTTPS request per `execute()`, to the one operator-pinned origin, over a fresh connection to an address resolved for that execution and proven publicly routable, TLS verified with `rejectUnauthorized: true` hardcoded (immune to `NODE_TLS_REJECT_UNAUTHORIZED=0`), no redirect, no retry, no reuse; completion declared only for 200, 201 and 204; outbound values are operator literals or approved `ValidatedExecutionAction` fields; post-send uncertainty is `execution_unconfirmed` (SEC-INV-062 … SEC-INV-069).
- **Does not guarantee:** system-wide egress control (SEC-INV-U03), provider-side exactly-once, reconciliation of `execution_unconfirmed`, protection from a provider that forwards the request, or anything about Pinata, Stripe, Sovereign Access, Content Protection or `enforce()`. It governs no other path retroactively.
- **Invariants:** SEC-INV-062 … SEC-INV-069, on top of every invariant of §5.3.

### 5.4 Sovereign Access provider execution

- **Guarantees:** its own authority model — an authoritative `EnterpriseAccessGrant` store read plus an active-status check — before minting a provider credential or enforcing a provider-side revocation.
- **Does not guarantee:** bounded-grant exercise semantics. It does not call the Kernel and does not read a `BoundedGrant`.
- **Explicit statement required by this document:**

  > The invariant "no adapter call without a usable bounded-grant exercise" (SEC-INV-011) **does not currently apply to Sovereign Access provider execution.**

- **Prompt 3 decided: record it as an explicit exception rather than converge it** (`NO_BYPASS_AUTHORITY_CONTROLLED_EXECUTION.md` §9.4). A **second** provider authority model reaching the same resource was also found there and is excepted on the same terms: Content Protection (`src/enterprise/content-protection`), gated only by a caller-asserted tenant scope (NB-003).
- **Invariants:** SEC-INV-019.

---

## 6. Root of Trust and Explicit Assumptions

These are boundary statements, not defects. They are recorded here so that no future security claim can omit them by accident.

| ID | Assumption | Consequence if violated | Evidence |
|---|---|---|---|
| SEC-TRUST-001 | **Host trust.** The Frontera host process, its environment variables, its process user and its data directory are inside the trusted computing base. | Total compromise: every HMAC signing secret, every API key, every provider credential, all in-memory recognition and authority state, the emergency-deny flag, and the ability to re-seal every digest and hash chain. | `THREAT_MODEL_V1.md` §2.6, §8.3 |
| SEC-TRUST-002 | **Privileged writer limit.** Authority artifacts carry unkeyed SHA-256 digests. These detect unauthorized mutation only where the attacker cannot rewrite artifact **and** digest together. | A writer able to re-digest can silently re-seal a store. Detection then depends entirely on out-of-band copies — backups, previously exported bundles. | `bounded-grant.ts:144-152` ("integrity, not a signature"); `THREAT_MODEL_V1.md` §7.3 |
| SEC-TRUST-003 | **Authoritative state durability.** Bounded grants are durable **when `persistence.provider === 'sqlite'`** and in-memory otherwise (the default); recognition, authority-graph, approval and handshake state are in-process `Map`s under every configuration. | On the in-memory store, authorization state is lost on restart and grant loss fails closed (`GRANT_EXERCISE_NOT_FOUND`). On the durable store, grants **and** revocations survive together (SEC-INV-035). Capability-token revocation is **not durable** under either. | `sqlite-bounded-grant-store.ts` and `in-memory-bounded-grant-store.ts` (the two `BoundedGrantStorePort` implementations); `composition-root.ts` (`buildBoundedGrantStore`); `capability-token-service.ts:47`; `kernel-provider-composition.ts:60-87`; `AUTHORITATIVE_GRANT_STORE.md` §15, GS-003 |
| SEC-TRUST-004 | **Voluntary chokepoint limit.** An authority gate constrains only the effects actually routed through it. Frontera's gate is not mandatory: nothing prevents in-process code from calling a provider directly. | An actor with code execution and network access reaches any provider without passing any gate. | No egress control exists; `packages/pinata-adapter`, `apps/agent-passport-web` call providers directly |
| SEC-TRUST-005 | **Process-isolation absence.** Frontera provides no process-level containment for governed agent code. It governs declared actions; it does not execute agent code, sandboxed or otherwise. | An agent sharing the Frontera process inherits every capability of that process, including ambient `process.env`. | `infrastructure/{terraform,docker,kubernetes}/` contain only `.gitkeep`; no sandbox primitive exists; `enforceAgentRuntimeGuard` (`runtime-guard.ts:246-270`) returns booleans and withholds nothing |
| SEC-TRUST-006 | **Provider adapter trust.** An `ExecutionAdapter` implementation is trusted code. The boundary constrains what data may cross it; it does not verify what the adapter then does. | A malicious or defective adapter can act outside the validated action it was handed. | `execution-adapter-port.ts:119-124` |
| SEC-TRUST-007 | **Deployment trust.** TLS, ingress restriction, rate limiting, WAF, secret storage and rotation, key custody and OS hardening are the operator's. Authentication is **off by default** (`AOC_ENTERPRISE_REQUIRE_AUTH`, default `false`). | An unauthenticated, cross-tenant API on all interfaces if deployed without the documented configuration. | `enterprise-configuration.ts:174`; `DEPLOYMENT_GUIDE_V1.md:101-102`; `THREAT_MODEL_V1.md` §8.1 |
| SEC-TRUST-009 | **Resolution authority trust (P12).** A composed `ExecutionResolutionAuthority` is trusted host code, trusted to answer provider truth about an execution Frontera could not observe. The port constrains what may cross it — a closed answer with no amount, asset, grant, budget or decision, no credential, no `providerRef` dereference by Frontera — and binds which authority may answer each execution; it does not verify that the answer is true. | A compromised or wrong authority can **falsely return** P7 capacity (a lying `confirmed-not-completed`) or **falsely retain** it and replay `executed` (a lying `confirmed-completed`). It cannot authorize, raise a ceiling, issue a grant, change an amount or reach the original effect. | `execution-reconciliation/authority.ts`; `ADR-EXECUTION-RECONCILIATION-AND-RESOLUTION-AUTHORITY.md` §3 |
| SEC-TRUST-010 | **MPP method and merchant mapping trust (P13).** A composed `MppChallengeMethodNormalizer`, the `resolveCounterparty` mapping and the `selectChallenge` policy are trusted host code: the normalizer is trusted to interpret its method's `request` (base units, currency → P9 asset), the resolver to map a merchant reference to the right Frontera counterparty. P13 constrains what may cross them — closed answers, exact P9 money, no authority field — but does not verify that the interpretation is true. | A wrong normalizer can propose the wrong amount or asset, and a wrong resolver the wrong counterparty; either is then **governed** — the Kernel, P10 ceilings and P7 limits still bound it — but not detected. Neither can authorize, name the business operation or reach an adapter. | `mpp-challenge/composition.ts`; `ADR-MPP-CHALLENGE-AND-BUSINESS-IDEMPOTENCY.md` §5 |

---

## 7. Unimplemented Security Invariants

These are the containment properties identified by the Prompt 0 baseline. **None is implemented.** They are recorded so they cannot be mistaken for guarantees, and so that each has an owner.

| ID | Desired invariant | Current state | Owner prompt |
|---|---|---|---|
| SEC-INV-U01 | No authority artifact is trusted without a verified signature from a key the application process cannot read. | **ASPIRATIONAL-UNIMPLEMENTED — unchanged by Prompt 4.** Unkeyed SHA-256 digests only; no signatures anywhere in `src/`. Digest verification is **not** signature verification. The attachment point is now exact: a detached signature over `serializeStoredGrantRecord`'s and `serializeStoredRevocationRecord`'s output, in a column beside each digest, verified in `verifiedGrant` / `verifiedRevocation` — three call sites, all already fail-closed (`AUTHORITATIVE_GRANT_STORE.md` §23). | Prompt 5 |
| SEC-INV-U02 | No production signing key is ever resident in application process memory. | ASPIRATIONAL-UNIMPLEMENTED. Every production key is a symmetric HMAC secret loaded from an environment variable. `AgentPassportSignerPort` is already KMS-shaped (async, opaque payload, no key material in the type). | Prompt 6 |
| SEC-INV-U03 | No egress to a provider except from an allowlisted adapter at a controlled network boundary. | ASPIRATIONAL-UNIMPLEMENTED — **unchanged by P6.** No allowlist, no egress control, no network namespace, firewall or sidecar. P6 added a third egress point, the Generic HTTP adapter's transport, which pins *its own* destination at the application level (SEC-INV-063 … SEC-INV-065); that constrains what that one adapter connects to, not what any other code in the process can reach, and the Pinata and Stripe paths are untouched (SEC-TRUST-004, SEC-TRUST-006). | Prompt 11 |
| SEC-INV-U04 | No governed agent executes in a process that holds any secret or any authority-write capability. | ASPIRATIONAL-UNIMPLEMENTED. No process boundary exists between governed and governing code. This is the dominating gap: U03, U04 and the sandbox contract are all consequences of establishing it. | Prompt 7 |
| SEC-INV-U05 | Every authority-bearing path honours a **single** durable kill switch. | PARTIALLY IMPLEMENTED — advanced, not closed, by Prompt 4. A durable `EmergencyControlReaderPort` now exists and **is** consulted by the bounded-grant / Governed Action path at four checkpoints, fail-closed, with SQLite persistence (SEC-INV-043 … SEC-INV-052). What remains unclosed is the word *single*: the older Action Enforcement path still uses its own process-local `setEmergencyDeny` instance field (`action-enforcement-runtime.ts:108`), which is not durable and is not wired to the new control plane; and the Sovereign Access and Content Protection provider paths honour neither. Convergence onto one durable control plane is deliberately out of Prompt 4's scope, because it is a behavioural change to an existing path. | Prompt 12 |
| SEC-INV-U06 | Aggregate behaviour is bounded, not only per-action behaviour. | ASPIRATIONAL-UNIMPLEMENTED **system-wide**. *Since P7*, one path is covered: when a deployment composes `exerciseControls`, host-declared aggregate count, aggregate amount and rolling-velocity limits hold on the bounded-grant exercise path (SEC-INV-070 … SEC-INV-079) — built **outside** layers B–E, in `src/features/exercise-control-runtime`, exactly as this row required; the execution runtime still holds no consumption model of its own and `riskScore`/`anomal`/`recommend` stay banned. No other path — `enforce()`, Sovereign Access, Content Protection, Pinata or Stripe outside ACE, application networking — has any aggregate bound, and there is no behavioural or anomaly analysis anywhere. | Prompt 13 |
| SEC-INV-U07 | No value matching a secret pattern is persisted into a Governance Record, whatever its key. | PARTIALLY IMPLEMENTED. Key-name redaction exists and runs before digesting (SEC-INV-030); value-pattern matching does not. | Prompt 15 |
| SEC-INV-U08 | Every layer that may not perform I/O has a capability-ban test. | PARTIALLY IMPLEMENTED. Five modules covered: context, obligation, grant, execution runtimes, and now the Kernel. Not covered: `src/features/action-enforcement`, the rest of `src/enterprise`, all of `packages/`, all of `apps/`. | Prompt 10 |

---

## 8. Claims Boundary

### 8.1 We Can Claim

Each of these is backed by an invariant above and, through it, by code or a test that fails CI.

1. Frontera never holds, requests, or can express a customer transaction signing key (SEC-INV-025).
2. On the bounded-grant exercise path, no external provider is contacted unless an authoritative grant re-read and a usable exercise assessment cover the exact action at that instant — with adapter invocation counts asserted for every refusal case (SEC-INV-011).
3. A caller never holds, presents, or can tamper with a grant: only an identifier crosses the boundary (SEC-INV-012, SEC-INV-026).
3a. When the durable bounded-grant store is configured, issued grants and revocations survive process restart with transactional persistence, authoritative re-read and fail-closed integrity validation — and restart can never increase authority (SEC-INV-035, SEC-INV-036, SEC-INV-037). This claim is void without the configuration clause.
4. On that path, revocation and expiry are effective immediately, with no cache and no background job (SEC-INV-013, SEC-INV-023).
5. Within the Frontera Enterprise authorization pipeline, the Kernel is the only decision producer, and contributing layers can only narrow or annotate (SEC-INV-001, SEC-INV-002).
6. `AocKernel.enforce()` never invokes the executor on a non-allow outcome (SEC-INV-009).
7. Layers C, D, E and the execution runtime perform no I/O of their own, evaluate no dynamic code, read no ambient clock or randomness, and depend on no background job (SEC-INV-020 … SEC-INV-023).
8. Provider credentials cannot appear on the Enterprise Host's public composition surface, and no hardcoded credential fallback exists (SEC-INV-028).
9. Governance records are append-only by interface, digest-chained, and redacted before digesting (SEC-INV-029, SEC-INV-030).
10. Every optional governance capability is byte-identically absent when unconfigured (SEC-INV-006).
11. Caller input cannot select which provider adapter an authorized action reaches: routing is trusted server-side configuration over fields the grant already contained, and one routing decision invokes at most one registered child, at most once, with the assessed action unchanged (SEC-INV-039 … SEC-INV-042).
12. When a deployment composes emergency control, an active applicable stop — or an emergency state that cannot be read — withholds execution at admission, at the bounded-grant commit boundary, at effect time after the authoritative grant re-read, and at the selected adapter; the commit-boundary read is synchronous by type; and none of it revokes a grant, rewrites a Kernel decision, or rewrites a recorded historical outcome (SEC-INV-043 … SEC-INV-052). This claim is void without the composition clause and is **path-local** to the bounded-grant / Governed Action path.
13. When a deployment composes customer identity admission and the Governed Action Orchestrator, `POST /api/governed-actions` admits only a bound customer credential — regardless of `AOC_ENTERPRISE_REQUIRE_AUTH` — derives actor and organization from that binding alone, rejects any caller attempt to name an identity, grant, adapter, provider or destination, reaches a provider only through the committed-decision → bounded-grant → exercise path, and returns no grant, digest, adapter identity or credential (SEC-INV-055 … SEC-INV-061). **Path-local** to that route; void without the composition clause.

14. When the governed-action path routes through a Generic HTTP adapter, that adapter can contact only its configured HTTPS origin, subject to its application-level public-address policy; it sends at most one request per execution, follows no redirect, retries nothing, never exposes its credential, and reports a post-send loss of certainty as `execution_unconfirmed` rather than as a failure (SEC-INV-062 … SEC-INV-069). **Component-local** to that adapter; void without the composition clause; not an egress control.

15. When a deployment composes P7 exercise controls, repeated use of a bounded grant is bounded by host-declared aggregate count, amount and rolling-velocity limits: no adapter runs without an atomic, durable reservation; a pending or unconfirmed effect keeps consuming; amounts are exact decimals in exact units; and the grant's authority binding is revalidated for exact equality immediately before execution (SEC-INV-070 … SEC-INV-079). **Path-local** to the bounded-grant exercise path; void without the composition clause; not distributed, not exactly-once, and not atomic with any external system.

16. When governed actions are composed, each governed action's lifecycle — committed decision, grant issuance and revocation, observed expiry, write-ahead claim, P7 reservation facts and execution outcome with its certainty intact — is recorded as an append-only, hash-chained, integrity-verified, tenant-confined stream, strictly after each fact, never read by anything that decides, and never awaited by anything that executes; a projection that fails **or never settles** changes no outcome, no adapter invocation and no capacity (SEC-INV-080 … SEC-INV-088). **Path-local** to the governed-action / bounded-grant lifecycle; void without the composition clause; integrity, not authenticity, and not completeness.

Every claim above must carry its scope when repeated. "No adapter call without a usable exercise assessment" is only true of the bounded-grant path, and must be stated that way.

### 8.2 We Cannot Claim Yet

1. **Rogue-agent containment.** Frontera governs declared actions; it contains nothing (SEC-TRUST-004, SEC-TRUST-005).
2. **Sandboxing or agent isolation.** No sandbox, no isolation primitive, no agent execution runtime (SEC-TRUST-005).
3. **System-wide adapter gating.** SEC-INV-011 is PATH-LOCAL. It does not hold for `enforce()`, for Sovereign Access, or for Content Protection (SEC-INV-010, SEC-INV-019, NB-003). Eight of fifty-five effect paths are bounded-grant controlled. P12 raised only the denominator, by two: its status query (EP-054) and P7 resolution row (EP-055) are deployment-gated trusted in-process paths, not bounded-grant ones. P7 raised both numbers by three, because its three new paths — the exercise-control reserve, settle and release writes (EP-051 … EP-053) — are local authority-state writes reachable only inside the bounded-grant gate, not provider effects and not any other path becoming governed. P6 raised both numbers by one because its one new path, EP-050, sits below the bounded-grant path, not because any other path became governed. Prompt 4 raised the denominator rather than the numerator; P5 raised both by one, because its one new path — the customer HTTP entry, EP-049 — is itself the bounded-grant path, not because any other path became governed (NO_BYPASS §5.11).
4. **Cryptographically authenticated authority artifacts.** Unkeyed digests only; no signatures anywhere in `src/` (SEC-TRUST-002, SEC-INV-U01).
5. **KMS/HSM-protected signing.** Every production key is a process-resident symmetric secret from an environment variable (SEC-INV-U02).
6. **One durable kill switch across every authority-bearing path.** A durable interlock now covers the bounded-grant / Governed Action path and fails closed when unreadable (SEC-INV-043 … SEC-INV-052, `AOC_EMERGENCY_CONTROL.md`), and it is opt-in. It does **not** cover `AocKernel.enforce()`'s Action Enforcement path — which keeps its separate, process-local, non-durable `emergencyDeny` — nor Sovereign Access, nor Content Protection. "Frontera has a global kill switch" is false; "the bounded-grant path has a durable, fail-closed operational stop when configured" is true (SEC-INV-U05).
7. **Behavioral abuse detection, and aggregate control beyond one path.** No behavioural or anomaly analysis anywhere, and the authorization layers are structurally banned from containing any. P7's host-declared count, amount and velocity limits hold **only** on the P7-enabled bounded-grant exercise path (SEC-INV-070 … SEC-INV-079); "Frontera bounds aggregate behaviour" is false, "the bounded-grant path bounds aggregate use of a grant when exercise controls are composed" is true (SEC-INV-U06).
8. **A hardened SaaS surface.** One blocker (APW-001, unauthenticated credential disclosure at checkout) is remediated and regression-tested, but APW-002, APW-003, APW-006 and APW-007 remain open in `AGENT_PASSPORT_WEB_THREAT_MODEL.md`.
9. **Complete repository threat-model coverage.** `src/enterprise` and `apps/agent-passport-web` are now modelled, but `packages/*` libraries remain outside every threat model (`THREAT_MODEL_V1.md` §9), and no independent penetration test has been performed against any surface.
10. **Durable authorization state by default.** A durable bounded-grant store now exists and is production-capable, but it is selected only when `persistence.provider === 'sqlite'`; recognition, approval, capability-token and policy-pack state remain in-process under **every** configuration (SEC-TRUST-003, GS-003). "All Frontera authority is now durable" is false.
11. **Anti-rollback on the authority store.** Restoring a database snapshot taken before a revocation restores the revoked grant, with every integrity check passing. Not implemented, not claimed (`AUTHORITATIVE_GRANT_STORE.md` §17, GS-002).
12. **Tamper-proof authority storage.** The store's digests are unkeyed; a writer who can re-seal defeats every check (SEC-TRUST-002, GS-001).
11. **Third-party-verifiable Agent Passports.** They are HMAC-signed; the registered "public key" is symmetric-scheme metadata and cannot verify them.
12. **Repository-wide capability restriction.** Proven for five modules; absent for `packages/`, `apps/` and most of `src/enterprise` (SEC-INV-U08).
13. **System-wide network egress control.** The Generic HTTP adapter pins its own destination; nothing prevents other process code — or the Pinata and Stripe paths — from reaching the network (SEC-INV-U03, SEC-TRUST-004). "Frontera blocks unauthorized egress" is false.
14. **Exactly-once external execution.** P7 adds a reservation keyed to the execution identity, so one identity cannot invoke an adapter twice on the P7-enabled path (SEC-INV-077) — still not exactly-once. P6 gives a durable write-ahead claim before any adapter invocation, at most one outbound request per `execute()`, no automatic retry, an operator-mappable provider idempotency value, and honest `execution_unconfirmed` reporting. It gives no provider-side idempotency guarantee, no reconciliation and no automatic recovery (SEC-INV-068, SEC-INV-069). P12 adds explicit, trusted reconciliation — a host-composed resolution authority may later establish whether an uncertain effect completed — but no automatic recovery, no retry and no exactly-once: an execution no trusted authority can answer stays unresolved (SEC-INV-111 … SEC-INV-122).
15. **Distributed or cross-host quota.** P7 serializes admission within one SQLite file on one host. Separate ledgers on separate hosts do not share a quota, there is no consensus, and there is no automatic abandoned-reservation recovery (SEC-INV-071, SEC-INV-072). Since P12 a reservation's capacity returns only through an explicit, trusted reconciliation that durably proved non-completion — never by time or restart (SEC-INV-117, SEC-INV-120).
16. **A tamper-proof, authenticated, complete or system-wide event record.** The P8 stream is tamper-*evident* with unkeyed digests — not signed, not WORM, not non-repudiable, not replicated — and it covers the governed-action / bounded-grant lifecycle only; `evaluate()`, `enforce()`, Sovereign Access, Content Protection and application code emit nothing into it. Its order is per stream, not global, and a projection that failed — or that was still queued when the process stopped — is not retried or reconciled, so it is neither exactly-once nor complete: verification proves the integrity and order of what is present, and an absent event is never evidence that its fact did not occur (SEC-INV-084, SEC-INV-085, SEC-INV-088).
17. **Authenticated MPP challenges.** A client cannot verify an MPP server's challenge binding (HMAC, AEAD or state); P13 validates structure, request binding, expiry and a trusted business interpretation, and the merchant verifies its own binding only when a credential is presented (P14). "Frontera verified the challenge is genuine" is false (SEC-INV-135, `ADR-MPP-CHALLENGE-AND-BUSINESS-IDEMPOTENCY.md` §4).
18. **Paying a real merchant.** P13 builds no Payment credential and ships no payment method: it ends at a durable, governed, idempotent handoff. Credential construction, Stripe and provider execution are P14.

---

## 9. Change Control

1. **Promotion requires proof.** An invariant's scope may only be widened — PATH-LOCAL to LAYER-LOCAL, LAYER-LOCAL to SYSTEM-WIDE — when code or a test actually proves the wider scope. Changing the prose is not promotion. A scope change with no corresponding test change should be rejected in review.
2. **Local invariants must never be represented as system-wide claims.** Any external artifact — architecture document, README, datasheet, sales material, model-facing summary — that repeats an invariant from §4 must carry its scope. SEC-INV-011 in particular must always name the bounded-grant path.
3. **Weakening requires review.** Removing an enforced invariant, or reducing its enforcement strength, requires an explicit architecture decision and a corresponding update to the tests named in its Evidence column. A test deleted without an entry here being updated is a regression, not a cleanup.
4. **New effect paths must declare themselves.** Any new path capable of producing an external effect must be added to §5 with its own guarantees and non-guarantees, **and to `NO_BYPASS_AUTHORITY_CONTROLLED_EXECUTION.md` §5 with its own `EP-` id and classification**, before it ships. A path absent from either is covered by no invariant here and by no claim there. `src/enterprise/__tests__/no-bypass-effect-paths.test.ts` fails the build when a new provider SDK site, execution-adapter invocation, route file or Server Action appears without that update.
5. **Boundary statements are not optional.** §6 travels with §4. A security claim quoting §8.1 without the relevant §6 assumption is incomplete.
6. **Unimplemented stays unimplemented until owned.** An entry in §7 moves into §4 only when its owner prompt lands the mechanism and the evidence.
